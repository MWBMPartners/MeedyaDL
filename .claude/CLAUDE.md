# MeedyaDL - Claude Code Project Context

## Project Overview

A multiplatform media downloader desktop application built with **Tauri 2.0 + React + TypeScript**. Currently supports Apple Music via [GAMDL](https://github.com/glomatico/gamdl), with planned support for additional services. Targets macOS (Apple Silicon), Windows (x64/ARM64), Linux (x64/ARM64/ARMv7), and ChromeOS (via Linux `.deb`).

## Architecture

- **Frontend**: React 19 + TypeScript + Vite + Tailwind CSS v4 + Zustand state management
- **Backend**: Rust (Tauri 2.0) with IPC command handlers
- **GAMDL Integration**: CLI subprocess calls (`python -m gamdl ...`) - never imported as a Python library
- **Multi-service architecture** (#107): Service-agnostic download pipeline with pluggable engines. `MediaServiceId` enum (Apple Music, YouTube Music, YouTube, Spotify, BBC iPlayer) detected from URLs. `engines.toml` defines engine metadata and per-platform engine chains (compiled into binary via `include_str!()`). `EngineRegistry` in `engine_registry.rs` provides runtime query layer (`resolve_engine()`, `resolve_engine_chain()`, `detect_platform()`). `EngineCommandBuilder` trait + `run_engine()` in `engine_runner.rs` abstract subprocess spawning with dual event emission ("engine-output" + legacy "gamdl-output"). Download queue items carry `service` and `engine` fields; enrichment pipeline guarded behind `is_apple_music` check. `PerServiceSettings` in `settings.rs` nests per-service config (Apple Music, Spotify stub, YouTube stub) with `engine_priority` overrides per platform. Engine fallback via `try_engine_fallback()` for platforms with multiple engines (e.g., BBC iPlayer: get_iplayer → yt-dlp). Frontend: `detectService()` and `parseMediaUrl()` in `url-parser.ts`, `getEngineConfig()` IPC, TypeScript types for all service settings.
- **Dependencies**: Self-contained in app data dir (Python via python-build-standalone, GAMDL via pip, tools)
- **System-Python reuse** (#1017): the setup wizard's Python step can DETECT a compatible system Python and REUSE it instead of downloading the portable runtime. `python_manager::detect_system_pythons()` probes PATH (`python3`, `python3.14…3.10`), Homebrew (`/opt/homebrew/bin`, `/usr/local/bin`), python.org framework (`/Library/Frameworks/Python.framework/Versions/*`), pyenv shims, and the Windows `py` launcher — running a one-line `sys.version_info`+`sys.executable` probe (2 s timeout, no shell), deduping by canonical path, filtering by the floor. **Floor = Python 3.10** (GAMDL 3.8.2+'s `_ammuxer` decrypt module is a `cp310-abi3` wheel — abi3 loads on 3.10+). Reuse means `provision_venv_from_system_python(app, interpreter)` runs `{interp} -m venv {app_data}/python` — a **venv is required** because Homebrew/distro Pythons are PEP 668 "externally-managed" (`pip install` into the base env is blocked). On **Unix** the venv exposes `bin/python3`+`bin/pip3` (identical to the portable layout → all `python -m gamdl`/`-m pip` call sites unchanged); on **Windows** the venv puts `python.exe` under `Scripts/`, so runtime resolution goes through the new venv-aware `platform::resolve_managed_python_binary()` (probes `Scripts/python.exe` when portable-root `python.exe` is absent; Unix delegates to `bin/python3`). All `gamdl_service` sites + `python_manager::check_python_status`/`get_installed_python_version` use the resolver; the pure `get_python_binary_path` is kept for the portable install-target + path-shape tests. A provenance marker `{app_data}/python/.meedya-python-source.json` (`PythonSourceRecord { source: "portable"|"system-venv", interpreter, version }`) is written by both provision paths; `update_checker::check_python_update` reads it and **suppresses the portable-`PYTHON_VERSION` "update" prompt for a system-venv** (else a user on system Python 3.10/3.11 would be nagged to "update" to portable 3.12.8, silently replacing their chosen interpreter). Missing `venv`/`ensurepip` (Debian's split `python3-venv`) surfaces an actionable error and the wizard falls back to the portable download. IPC: `detect_system_pythons`, `use_system_python(interpreter)` in `commands/dependencies.rs`. Frontend: `PythonStep.tsx` offers per-interpreter "Use this" rows + a "Browse…" picker (mirrors tool Browse #278) + the portable-download fallback; `SystemPython` TS type, `dependencyStore.{systemPythons, isDetectingPythons, detectSystemPythons, useSystemPython}`.
- **Theming**: Platform-adaptive CSS custom properties (macOS/Windows/Linux themes)
- **Error Handling**: ErrorBoundary in main.tsx catches React crashes; unhandled rejection handler logs async errors. All frontend errors are persisted to Rust crash reports via `log_frontend_error` IPC command.
- **Logging**: `tracing` ecosystem (replaces `env_logger`) with dual output: stderr + daily-rotating file in `{app_data_dir}/logs/`. Compatible with `log` facade -- all `log::*!()` macros work unchanged.
- **Error Reports**: JSON files in `{app_data_dir}/crashes/`. Captures Rust panics (`setup_panic_handler()`), frontend errors (IPC), and download errors (terminal failures after all fallbacks/retries exhausted — network-category errors are excluded since they indicate connectivity issues, not app bugs). Auto-cleanup of reports older than 30 days. Users can report errors to GitHub Issues via `build_github_issue_url()` in `crash_report_service.rs` -- constructs a pre-filled issue URL with percent-encoded data, opened in the system browser. Title prefix and labels adapt to source type (`[Crash Report]` + `crash-report` label for panics/frontend errors; `[Error Report]` + `error-report` label for download errors). Privacy-first: `CrashReportDialog` shows a consent modal before submission. Backtrace truncated at 3500 chars for URL length safety. Issue template at `.github/ISSUE_TEMPLATE/crash-report.yml`.
- **Pre-download checks**: Three gates in `DownloadForm.tsx` before queueing: (1) Internet connectivity check (`check_internet_before_download` — multi-provider, multi-tier probe. Tier 1 (general internet): Cloudflare `1.1.1.1` → Google `google.com` (provider-neutral, sequential — first success short-circuits). Tier 2 (service-specific, only if Tier 1 passes): Apple Music API `amp-api.music.apple.com`. Three outcomes: Tier 1 fails = general "no internet" warning, Tier 1 passes but Tier 2 fails = "Apple Music API unreachable (internet is working)" warning, both pass = no warning) — if offline, the download is still queued but auto-start is skipped via `skip_auto_start` param on `start_download`, and a warning toast is shown; (2) Output path writability check (`check_output_path_before_download` — probes write access via temp file creation with 5s timeout) — **blocking**: prevents queueing if output path is inaccessible (catches disconnected cloud mounts, full disks, permission issues). Uses `spawn_blocking` + `tokio::time::timeout` to avoid hanging on unresponsive FUSE mounts. OS-specific error messages for ETIMEDOUT (errno 60, CloudMounter), ENOSPC, EROFS, ESTALE; (3) Cookie validation (`check_cookies_before_download`) — blocks if cookies expired/missing (skipped when offline). Cookie check is also skipped for wrapper users. Queue pre-flight also runs the output path check alongside internet/wrapper/cookie checks.
- **Toast deduplication**: Toasts with the same `message` are not duplicated on screen. Keyed toasts (optional `key` field) support category-based replacement and programmatic dismissal via `removeToastsByKey()`. Preflight warnings use keyed toasts with `preflight-cleared` events for auto-dismissal when checks pass.
- **Auto-retry without wrapper**: `auto_retry_without_wrapper` setting (default: `false`). When enabled and a wrapper download fails terminally, the queue automatically re-queues the item with wrapper disabled (cookie-based auth). Toggle in Settings > Advanced > Wrapper section (only visible when `use_wrapper` is true). Logic in `download_queue.rs` in both the `Err` path (non-zero exit) and success-path terminal (exit 0, no output) calls `retry_without_wrapper()` and re-processes the queue. Comprehensive `log::debug!()` tracing shows wrapper URL, setting value, item state, and retry result.
- **Wrapper m3u8 address** (GAMDL v3.1+, #605): `wrapper_m3u8_ip` setting (default: `"127.0.0.1:20020"`). When `use_wrapper=true` on GAMDL v3.1+, GAMDL no longer reads the HLS master playlist URL from Apple's API response — it opens a TCP connection to this address and receives the m3u8 URL from the wrapper service. Emission is double-gated: (a) `settings.use_wrapper == true`, (b) `gamdl_capabilities::supports(GamdlFeature::WrapperM3u8Ip)` returns `true` (i.e., the detected GAMDL release is 3.1+). Lives in both CLI (`--wrapper-m3u8-ip`) and INI (`wrapper_m3u8_ip`). Preflight check `check_wrapper_m3u8_health()` in `health_check_service.rs` does a 3-second TCP connect to the configured `host:port` before download, emitting a `PreflightCheck::WrapperM3u8` warning toast on failure. Configurable in Settings > Advanced > Wrapper (only visible when `use_wrapper` is true).
- **Network retry logging**: `try_network_retry()` returns `Option<(u32, u32)>` — the current attempt number and total attempts (initial + retries). Activity log differentiates error source: "Network error (via wrapper) — retrying (attempt {n} of {total})" vs "Network error (direct to Apple Music) — retrying (attempt {n} of {total})". Wrapper URL is surfaced in the activity log on network errors ("Network error occurred while using wrapper at {url}"). Default: 4 retries (5 total attempts).
- **I/O error handling**: Filesystem errors (disconnected cloud mounts, full disks, stale NFS handles) get a dedicated `"io"` match arm in the error handler with an actionable activity log message: "Filesystem error — check that the output directory is accessible and writable: {error_msg}". Detected by `is_io_error()` which matches `[Errno 60]` (macOS ETIMEDOUT), ENOSPC, EROFS, ESTALE, and generic I/O keywords. Not retried — user must fix the underlying issue.
- **Activity Log + Progress Bar refactor (Phase 3.5, #714)**: Six-commit holistic refactor landed in v1.0.1 prep. `services/progress_stages.rs` is the **single source of truth** for the 8 ordered per-item processing stages (`ProgressStage::{Metadata, WordLyrics, LyricsConversion, AnimatedArtwork, AcoustId, ReplayGain, MusicVideoDiscovery, Finalising}` — each with `weight()` + `label()`, covered by 6 unit tests for monotonicity / ellipsis convention / `ALL`-array completeness). Replaces the previous 8 scattered `PROGRESS_*_STAGE: f32` constants. Two shared helpers `set_stage(app, queue, dl_id, stage)` and `set_stage_with_label(app, queue, dl_id, stage, custom_label)` are callable from **both** the enrichment task AND the companion task, fixing the pre-3.5d bug where the closure-local `set_label` left the per-item bar caption stale on "ReplayGain loudness analysis…" while companion lyrics conversion ran for 30+ minutes (#712). Sub-stage labels now exist for lyrics format conversion (Enhanced LRC / WebVTT / Rich SRT / ASS / subtitle embed), MusicBrainz lookup, manifest write, content-rating rename, artist promo video, and the per-tier companion download phases. The 4 emission helpers (`emit_download_log`, `emit_app_log`, `emit_verbose_download_log`, `emit_verbose_app_log`) collapsed to facades over a shared `emit_inner` (Phase 3.5c) so future emission rules touch one place. New `emit_subprocess_line(app, dl_id, stream, line, show_in_ui)` helper centralised the four direct `app.emit("activity-log", …)` sites (Phase 3.5e). New `process::humanise_codec_skip_line()` (Phase 3.5h) strips GAMDL's noisy `(media ID: NNN)` and Python-repr codec list from "Skipping … format is not available" lines before activity-log emission. Companion task now snapshots the audio-file count BEFORE running GAMDL and emits "no compatible format available — skipped" instead of "Companion download complete" when zero new files landed (Phase 3.5h, prevented misleading "complete" messages when GAMDL exits 0 but skipped every track). Frontend caption assembly extracted to `lib/progress-caption.ts::formatActiveItemCaption()` (Phase 3.5f) so caption rules are unit-testable in isolation.
- **Library Scan page** (#717, all sub-features shipped + #766 Phase 2 of 5b): `library` page in the AppPage union, sidebar nav between Queue and History, component at `src/components/library/LibraryScanPage.tsx`. Wraps `scan_folder_for_manifests` (#456) in a first-class UI: folder picker → scan → results table with Artist / Album / track count / on-disk file count / detected codec / last-download date / per-row badges / Re-download action. Sub-feature landings: **5a** scaffold; **5b** smart-retry diff (`diff_library_scan_manifest` IPC → `services/smart_retry_planner.rs::plan_retry` → `LibraryScanDiff::{Plan, PartialCodecs, AllPresent, NotApplicable}`) — `DiffBadge` renders amber "N of M missing" / amber "N codec(s) missing" / green "All present" / grey "Cannot diff"; **5c** Apple Music `lastModifiedDate` freshness (`check_library_scan_freshness` IPC → `LibraryScanFreshness::{Fresh, Updated, Unknown}`, throttled to 5 concurrent in `runFreshnessChecks`) — `FreshnessBadge` renders accent "Content updated" tooltip with both dates; **5d** `MvGapFillModal.tsx` with a 2×2 prompt branch (MV-enabled-in-settings × user-says-yes) producing `null | true | false`; **5e** `mv_companion_override: Option<bool>` on `DownloadRequest` (`models/download.rs:110`), captured at enrichment-spawn time (`download_queue.rs`), gates Step 6 of the enrichment pipeline; **5f** Re-download button in `LibraryScanPage.tsx` enabled when `diff.kind === 'plan' || diff.kind === 'partial_codecs' || freshness.kind === 'updated'`, calls `startDownload({ urls, mv_companion_override })`; **5g** 10 unit tests in `MvGapFillModal.test.tsx`. **Codec-aware diff (#766, Phase 2 of 5b)**: `ManifestSource.companion_tiers: Option<Vec<Vec<String>>>` records the codec-registry IDs the user planned at download time (tier 0 = primary, tiers 1..N = `plan_companions()` output); populated by `build_manifest_companion_tiers()` in `download_queue.rs` and serialised through the `write_manifest` call site. Planner extensions in `smart_retry_planner.rs`: `find_missing_tier_codecs()` cross-references the tier plan against a `scan_suffix_inventory()` `BTreeSet<String>` of on-disk codec suffixes; `detect_codec_suffix()` does longest-match against `CODEC_REGISTRY.audio[*].suffix`. Empty-suffix codecs (e.g. `aac-hq` → clean filename) match files with no bracketed suffix; multi-codec fallback chains (`["aac-hq", "aac-mq"]`) report missing only when BOTH variants are absent. Track-level `Plan` takes precedence over codec-level `PartialCodecs` (a missing track is the more severe gap). Backward-compat: `companion_tiers: None` on manifests written before #766 skips the codec diff entirely and the planner returns its pre-#766 `AllPresent`. Forward-compat: unknown codec IDs (a manifest written by a future MeedyaDL with a codec our local `codecs.toml` doesn't recognise) are silently skipped instead of reported missing — reporting them would force a redundant re-download. Frontend codec-display map at `LibraryScanPage.tsx::CODEC_DISPLAY_NAMES` renders user-friendly names ("Dolby Atmos", "AAC", "AAC Legacy") in the badge tooltip. Re-download retry path runs the album URL with GAMDL `overwrite=false`, so successful primary files are preserved while only the missing codec variants are re-fetched. **Still out of scope (#667 Phase 3)**: per-track-per-codec granularity ("tracks 12–45 missing AAC, 1–11 have it"); today's `PartialCodecs` reports at album-level codec granularity.
- **Activity Log**: App-wide via shared `utils/activity_log.rs` module. Two emission helpers: `emit_download_log(app, download_id, message)` for per-download events, `emit_app_log(app, message)` for system-level events (uses `download_id: "system"` sentinel). Frontend renders `[System]` badge in `text-status-info` colour for system entries. Covers: update checks, dependency installs, cookie imports, settings saves, queue operations (enqueue/cancel/retry/clear/export/import), login window, pre-flight checks, app startup, and queue recovery. The `ActivityLogEvent` struct and helpers were refactored from `download_queue.rs` private scope to shared utility for cross-module use. **Capped at 10,000 entries** (`MAX_ENTRIES` in `activityStore.ts`) — when exceeded, oldest entries are trimmed. Entries have auto-incrementing `_id` for stable React keys. **Virtualized rendering** via `@tanstack/react-virtual` — only ~150 DOM nodes rendered regardless of entry count (previously rendered all entries, causing 14+ GB WebView RAM at 7,500 entries). **RAF-batched event ingestion**: `App.tsx` buffers incoming `activity-log` events and flushes them in a single `addEntries()` call per animation frame (~60/s max store updates vs hundreds/s previously). **Backend `\r` coalescing**: `download_queue.rs` stdout/stderr readers only emit the last `\r`-separated segment to `activity-log` (5-10x event reduction for progress-heavy downloads); **verbose mode bypasses `\r` coalescing** so all progress lines are emitted when `verbose_activity_log` is enabled. Track separator lines include artist and album context from the queue item. Queue item demarcation: separator line emitted at start of each download. Export filename: `MeedyaDL-activity-log_YYYY-MM-DD_HHhMMm.log` (`.log` extension, timestamped).
- **Persistent on-disk activity log** (#541): `services/activity_log_writer.rs` streams every `ActivityLogEvent` to `{log_dir}/activity-YYYY-MM-DD.log` via a buffered Tokio background task. `start(log_dir, shutdown)` spawns the writer and returns an `ActivityLogWriterHandle` that wraps `UnboundedSender<ActivityLogEvent>`; `utils::activity_log::register_disk_writer()` stores the handle in a `OnceLock` so `emit_download_log`, `emit_app_log`, `emit_verbose_download_log`, and `emit_verbose_app_log` all fan out to disk after emitting the Tauri event. The four direct-emit sites in `download_queue.rs` (`emit_companion_stream_line`, stdout/stderr readers, track separator) call `write_to_disk(&event)` alongside their `app.emit("activity-log", &event)`. **Verbose events are written to disk even when the UI is filtering them** — the file is the forensic record. Writer task uses `tokio::select!` over `rx.recv()` and a 500 ms flush tick, polls the shared `ShutdownSignal` to flush-and-exit cleanly, and detects UTC date rollover at write time. Unit tests (`activity_log_writer::tests`) cover file creation, line format, rollover, and `list_log_files()` ordering. Retention 7 days via `clear_old_logs()` in `lib.rs` (which also scans the override dir when set). IPC in `commands/activity_log.rs`: `export_disk_activity_log(days_back)` (native save dialog, default 3 days, concatenates newest-first files in chronological order with header banners) and `get_logs_folder_path()` (returns active dir, creating if absent — frontend pairs with `@tauri-apps/plugin-shell`'s `open()` to reveal). Frontend: **Export Disk** + **Reveal** buttons in `ActivityLog.tsx` header; existing in-memory **Export** preserved. User-configurable location via `activity_log_path_override: String` setting (default empty = `{app_data_dir}/logs/`). `lib.rs::resolve_activity_log_dir()` tries `create_dir_all` on the override and falls back to default with `log::warn!` on failure. UI: Browse + Reset buttons in Settings > Advanced > Diagnostics; applies on next app restart (writer owns the file handle for the process lifetime). `ShutdownSignal::flag()` exposes the inner `Arc<AtomicBool>` so the writer can poll shutdown without holding a Tauri `State`.
- **Error classification**: `process::classify_error()` returns category strings used for retry strategy and UI display. Categories: `auth`, `io`, `network`, `codec`, `not_found`, `rate_limit`, `tool`, `unknown`. Network detection covers: "network", "timeout", "timed out", "connection", "connecterror", "dns", "httpx", "httpcore". The httpx/httpcore keywords catch any error from Python's HTTP transport libraries (including raw traceback frames containing these library paths).
- **Error extraction**: In `download_queue.rs`, `extract_python_exception()` is ALWAYS tried against raw stderr (not just when `errors.last()` contains "traceback"). This handles cases where the output parser captures a traceback frame as the last error instead of the actual exception. The output parser's Priority 7 keyword matching skips lines starting with `File "` (Python traceback frames) to prevent function names like `map_httpcore_exceptions` from triggering the "exception" keyword.
- **Sentry** (opt-in): `sentry_enabled` setting (default: `false`). When enabled, Rust SDK + JS SDK send anonymous crash data. Toggle in Settings > Advanced. Exists as a parallel path alongside the GitHub Issues reporting.
- **Cancellation handling**: When a download is cancelled, the error handler short-circuits on the sentinel string `"Download cancelled by user"` — it only decrements `active_count` without overwriting the already-set `Cancelled` state or creating an error report.
- **URL validation**: URLs are validated as starting with `http://` or `https://` before being passed to GAMDL subprocess in `gamdl_service.rs`. Prevents `--`-prefixed strings from being misinterpreted as CLI flags.
- **Credential redaction**: `redact_url_query()` strips query parameters from wrapper URLs before logging, preventing authentication tokens from persisting in plaintext log files.
- **Timeout safety**: All HTTP clients use explicit timeouts — `download_file()` uses 30s connect timeout, health check uses 5s, GitHub API uses 15s, `pip show gamdl` uses 10s. Prevents indefinite blocking on unresponsive servers or network mounts.
- **Log retention**: Tracing logs cleaned up after 7 days, session logs (trimmed activity entries) after 30 days, by `clear_old_logs()` at startup. Session logs preserve debugging data from trimmed in-memory entries via `save_session_log` IPC command.
- **IPC rate limiting**: Sliding-window rate limiter in `utils/rate_limiter.rs` applied to `start_download` (10/min), `check_all_updates` (1/min), `download_and_install_app_update` (1/min), `import_cookies_from_browser` (3/min).
- **Settings integrity**: SHA-256 checksum written to companion `settings.json.sha256` on save, verified on load. Mismatch logs warning but loads settings (backwards compatible).
- **Settings migration**: `settings_version` field with `migrate_settings()` in `config_service.rs`. Sequential migrations (v0→v1, etc.) run on load. `CURRENT_SETTINGS_VERSION` constant.
- **After-queue actions**: `AfterQueueAction` enum (do_nothing, open_output_folder, play_sound, close_meedyadl, restart_computer, hibernate_computer, shutdown_computer) with persistent + one-shot modes. Settings dropdown in General > Preferences, right-click context menu on Download page, status bar indicator.
- **Notification tiers**: Error/warning toasts are persistent (no auto-dismiss), info/success respect configurable `notification_auto_dismiss_seconds` (3-60s, default 5). Backend notification throttling batches rapid notifications of the same category (10s window). The duplicate-URL toast in `DownloadForm.tsx` uses `'info'` (auto-dismisses) rather than `'warning'` because the download is still queued and no user action is required (#657).
- **Native notification preflight** (#658): The Tauri `tauri-plugin-notification` JS API's `requestPermission()` only triggers the macOS system prompt the first time it is called for a bundle ID — if the user dismissed (rather than granted/denied) the original prompt, every subsequent call resolves silently with `'default'` and `sendNotification()` becomes a no-op. To work around this, `App.tsx` Effect 2 runs a one-shot `isPermissionGranted` → `requestPermission` preflight after settings load, *unless* `notification_style == 'in_app_only'`. The result is logged to the WebView console (`[notification] startup permission request resolved: …`). The `addToast` notification path in `uiStore.ts` no longer swallows errors silently — failures are surfaced as `console.warn`. A "Send Test Notification" button in Settings > General > Notifications lets users verify the OS pipeline on demand. Backend `send_desktop_notification` in `download_queue.rs` also gates on `notification_style != "in_app_only"` so the user's preference is honoured by the Rust completion path too (previously it only checked `desktop_notifications: bool`).
- **Fallback chain UI — remove + re-add** (#659): `FallbackChainList<T>` accepts an optional `allItems: readonly T[]` prop. When supplied, each row gets an `X` (remove) button and an "Available (not in chain)" panel renders below the active list with `+` (re-add) buttons. The remove button on the only remaining row is disabled — an empty chain would block every download. No settings schema migration is needed: the chain remains a `Vec<SongCodec>` / `Vec<VideoResolution>`; removed items are simply absent from the array. `merge_options()` and `try_fallback()` already handle arbitrary chain lengths. `FallbackTab.tsx` builds the universe via `Object.keys(SONG_CODEC_LABELS) as SongCodec[]` (and the analogous `VIDEO_RESOLUTION_LABELS`) into module-level `ALL_SONG_CODECS` / `ALL_VIDEO_RESOLUTIONS` constants. `QualityTab` callers (artist auto-select, video codec priority) intentionally do NOT pass `allItems` and continue to render as pure-reorder lists.
- **Python traceback noise suppression** (#660): GAMDL and its Python deps (`httpx`, `async_lru`, `gamdl.interface`) raise multi-line tracebacks on certain code paths (notably music-video cover-art fetch). The activity log used to render two red Error entries per traceback — the `Traceback (most recent call last):` header (matched by Priority 7's `traceback` keyword) and the exception summary line (matched by `PYTHON_EXCEPTION_REGEX`). The header was duplicate noise; multiplied across many retries it produced 40+ lines for a single recurring upstream bug. Fix layered three ways: (a) new `GamdlOutputEvent::TracebackFrame { raw: String }` variant captures the header, `File "..."` stack frames, and caret highlight lines explicitly; (b) the `traceback` keyword is removed from Priority 7 — the explicit variant supersedes it; (c) new `process::is_python_traceback_noise(line)` helper (cheap, no-regex twin of the Priority 3c branch) is called by the stdout/stderr readers in `download_queue.rs` to gate the per-line `activity-log` Tauri event in non-verbose mode. The on-disk activity-log writer still records every line (forensic record). The actual exception summary (`TypeError: …`, `httpx.ConnectError: …`) remains a real Error event and is unaffected.
- **Terminal-state guards** (#661): `set_complete()` and `set_error()` on `DownloadQueue` refuse to overwrite terminal states. `set_complete` skips when state is `Error` or `Cancelled`; `set_error` skips when state is `Complete` or `Cancelled`. Without these guards, the per-item completion task at the bottom of `process_queue()` would always call `set_complete` after the post-companion advisory pass, silently flipping a previously-failed item back to `Complete` in the UI ("revival"). Likewise, late-arriving subprocess errors during cancellation teardown could overwrite `Cancelled → Error` and poison the error field. Skipped transitions emit a `log::debug!`. Tests: `set_complete_does_not_revive_errored_item`, `set_complete_does_not_revive_cancelled_item`, `set_error_does_not_overwrite_cancelled_item`, `set_error_does_not_overwrite_complete_item` in `download_queue::tests`. Companion-timeout activity-log message also corrected from the misleading `"marking complete"` to `"skipping remaining companions; final tag pass still to run"`, and a new `Final tag pass: applying [Explicit]/[Clean] suffixes…` log entry fires at the start of the post-companion advisory pass so the long silent gap becomes visible.
- **Cooperative-cancel for sync companion code** (#663): `tokio::task::JoinHandle::abort()` only takes effect at an `.await` point — it cannot preempt a synchronous function. The companion task's `run_companion_lyrics_conversion` (sync `fn`, recursive `find_dirs_with_ttml` walk + per-album-dir TTML→LRC/SRT/VTT/ASS conversion) used to keep emitting `Companion: converted N TTML file(s) to Enhanced LRC` activity-log events for 5–15 minutes *after* the deadline timeout fired and `handle.abort()` was called — the user-visible "spring back to life" symptom on items already marked Complete. Fix: `spawn_companion_downloads` now returns a `CompanionTaskHandle { handle: JoinHandle, aborted: Arc<AtomicBool> }`. The completion task's deadline path calls `handle.abort()` (the wrapper method, which sets the flag *and* aborts the runtime task) before awaiting `handle.handle`. The companion tier loop checks the flag between iterations (so we don't launch another GAMDL after timeout); `run_companion_lyrics_conversion` checks it on entry and between every album_dir iteration in the conversion loop. When the flag fires, a single `Companion task aborted — skipping remaining post-processing` line is emitted and the function returns. **Pattern to remember**: any new sync function called from inside an async tokio task must accept and check an `Arc<AtomicBool>` cooperative-cancel flag if it can take longer than the parent task's expected lifetime.
- **BPM analysis**: `bpm_service.rs` with two-phase approach (#418): (1) checks existing BPM tags via ffprobe, (2) if absent, analyses audio via FFmpeg `silencedetect` filter (`aresample=22050,lowpass=f=300,highpass=f=40,silencedetect=noise=-30dB:d=0.1`) for onset detection. Writes to M4A (`tmpo` atom), FLAC (`BPM` Vorbis Comment), MP3 (`TBPM` ID3v2 frame). Full DSP analysis deferred to MeedyaSuite-core `meedya-audio-analysis` module. `bpm_analysis_enabled` setting (default: false).
- **Disk space check**: `check_output_path()` preflight now checks available space via `fs2::available_space()`. Warns if < 500 MB remaining.
- **macOS updater artifact naming**: Tauri 2.x names the macOS updater bundle `MeedyaDL.app.tar.gz` (after the `.app` bundle, no arch/version suffix). The `release.yml` upload step and `latest.json` rebuild must use this exact filename. The `fix-updater-manifest.yml` standalone workflow uses the same filename. Previous incorrect filename `MeedyaDL_aarch64.app.tar.gz` caused `darwin-aarch64` to be missing from `latest.json`, breaking macOS in-app updates (#368).
- **CI hardening**: `workflow_dispatch` inputs in `release.yml` and `version-bump.yml` are passed via `env:` variables, not direct `${{ }}` shell interpolation, preventing injection attacks from collaborators. All release-critical GitHub Actions are pinned to immutable commit SHAs (not mutable version tags like `@v4`) across all 5 workflow files — prevents supply chain attacks where a compromised action tag could inject malicious code into CI/CD pipelines. `cargo-deny` licence scanning via `deny.toml` runs in the CI backend job to enforce a permissive licence allowlist. CodeQL workflow requires `actions: read` permission alongside `security-events: write` and `contents: read` for the analyze job to access workflow run telemetry.
- **INI injection prevention**: `sanitize_ini_value()` in `config_service.rs` strips `\n` and `\r` from all user-provided string values before writing to GAMDL's config.ini. Prevents INI key injection via crafted settings import files.
- **HTML sanitization**: `rehype-sanitize` in `HelpViewer.tsx` strips dangerous HTML elements (script, iframe, event handlers) while allowing safe ones (details, summary) via a custom allowlist schema.
- **Import validation**: `sanitize_imported_settings()` in `commands/settings.rs` truncates paths (1024), URLs (2048), templates (512), language (20), storefront (10), and strips control characters from all imported fields.
- **Atomic file writes**: Both `settings.json` and `queue.json` use write-to-temp-then-rename pattern (`std::fs::write` to `.json.tmp`, then `std::fs::rename`) for crash/power-loss safety.
- **Queue debounce**: `save_queue_to_disk()` saves at most once per 500ms via `AtomicU64` timestamp, with a delayed follow-up save to ensure final state is persisted. Reduces I/O pressure from rapid queue mutations.
- **No shell interpolation**: All subprocess calls use parameterised `Command::new().arg()` — no `sh -c format!()` patterns. The two former `sh -c` invocations in `dependency_manager.rs` (GPAC cpio extraction and .deb tar extraction) were replaced with direct process invocations.
- **Modal focus trap**: `Modal.tsx` has `role="dialog"`, `aria-modal="true"`, `aria-labelledby`, Tab/Shift+Tab focus cycling, auto-focus on open, and focus restore on close.
- **SECURITY.md**: Vulnerability reporting policy with responsible disclosure instructions, supported versions, and comprehensive security measures list.
- **Premium feature token resolution**: `TokenSource` enum in `apple_music_api.rs` with 3-tier priority for features that need a developer token (syllable-lyrics, animated artwork, music video relations): (1) User-provided MusicKit credentials (`UserProvided`), (2) Compile-time embedded token (`option_env!("APPLE_DEVELOPER_TOKEN")`) (`Embedded`), (3) Web player-extracted token from OS keychain (`WebPlayer`). `resolve_premium_feature_token()` returns `Option<(String, TokenSource)>`. The web player token is opportunistically extracted during Apple Music login via `login_window_service.rs` — JavaScript evaluation on the WebView captures the `MusicKit.getInstance().developerToken` value, which is then stored in the OS keychain (`webplayer_developer_token` key) via `keyring` crate. Token is NOT used for basic catalog API calls (those always use user-provided MusicKit JWT); only for premium features as a last-resort fallback. IPC commands: `has_webplayer_token` (checks keychain), `clear_webplayer_token` (removes from keychain) in `commands/credentials.rs`.
- **Dev access mode**: Hidden developer tools activation via Konami code sequence (arrow keys + BA) in the frontend. `useKonamiCode.ts` hook listens for the key sequence and triggers a passphrase modal. Passphrase validated via SHA-256 hash comparison (hash stored in code, never the plaintext). On successful activation: sets `dev_access_enabled: bool` in AppSettings + writes a keychain sentinel (`dev_access_sentinel` key). `check_dev_access` IPC reads the keychain sentinel (survives settings reset). `activate_dev_access` / `deactivate_dev_access` IPC commands toggle both settings and keychain. Gates a "Developer Tools" section in `AdvancedTab.tsx` (`DevToolsSection` component) showing: web player token status (present/absent + clear button), token source hierarchy visualization, and diagnostic info. Section only renders when `dev_access_enabled` is true.
- **Regex caching**: `parse_apple_music_url()` in `apple_music_api.rs` uses `static LazyLock<Regex>` for one-time compilation instead of per-call `Regex::new().expect()`.
- **SHA-256 checksum verification**: `download_file()` in `archive.rs` computes SHA-256 during streaming download (zero extra I/O pass) using the `sha2` crate. Returns `(bytes_downloaded, sha256_hex)`. `download_and_extract_verified()` accepts an optional expected hash and verifies after download, before extraction — mismatch deletes the temp file and returns an error. Infrastructure-only for now; callers can opt in by passing `expected_sha256`.
- **Graceful shutdown**: `ShutdownSignal` in `download_queue.rs` wraps `Arc<AtomicBool>` with `trigger()` / `is_triggered()` methods. Registered as Tauri managed state. Triggered on `WindowEvent::Destroyed` and tray "quit" action. Checked at iteration boundaries in enrichment pipeline (between 8 stages), companion download loops (between tiers), and lyrics companion loops (between formats). Fire-and-forget `tokio::spawn` tasks exit early instead of starting new work after shutdown.
- **Download manifest files (.meedyadl)**: After enrichment, a `manifest.meedyadl` JSON file is written to each album output directory via `write_manifest()` in `download_queue.rs` (renamed from hidden `.meedyadl` dotfile in #447; auto-migration moves old files). Atomic writes via `.tmp` + rename. Records source URL, platform, storefront, `lastModifiedDate` (for content refresh detection), per-track metadata (ISRC, title, individual URLs). Multi-platform merge-safe — new platform sources append to existing manifests. MIME type: `application/vnd.mwbmpartners.meedyadl.download+json`. Import via "Import" button on Download page (`import_manifest` IPC command), drag-and-drop `.meedyadl` files, or OS file association (deep link handler). **Folder scan** (#456): `scan_folder_for_manifests` IPC command recursively scans a user-selected directory (depth limit 10) for manifest files, extracts URLs/artist/album/codec/lastModifiedDate for re-downloading. "Scan" button on Download page. `ScannedManifest` struct includes `current_codec` (detected from M4A tags) and `audio_file_count` for quality upgrade (#380) and content refresh detection. File association registered in `tauri.conf.json`. Schema version 1 with `sources[]` array (distinct from queue export which uses `items[]`). `downloaded_at` captures download start time, `codec` defaults to null (manifest is a metafile, not a quality prescription). Document type icons: `icon-doc.svg/png/icns/ico` in `assets/brand/`.
- **Output detection fallback**: `find_album_directory()` uses artist/album hints (#452) — three-tier strategy: (1) targeted path match (`base_dir/Artist/Album/`) with case-insensitive fallback via `find_directory_case_insensitive()`, (2) `find_deepest_audio_dir()` recursive timestamp scan, (3) terminal failure. `has_direct_audio_files()` checks for audio files non-recursively. Prevents cross-contamination between concurrent downloads by matching the specific artist/album rather than picking the most recently modified directory. Runs before terminal failure check when GAMDL exits 0 without "Saved to:" lines (GAMDL 2.9.x with native `--song-codec-priority` for albums).
- **Metadata cross-contamination defence** (#452): Three layers: (1) targeted `find_album_directory()` with artist/album hints, (2) depth-limited `collect_m4a_depth_limited()` (max_depth=1) prevents collecting files from sibling directories, (3) album-name + artist-name validation in `enrich_single_file()` before writing API metadata — mismatches logged and skipped.
- **Serial queue processing** (#455 / #706): Queue processes one item at a time — the ENTIRE pipeline (GAMDL download → companions → enrichment → lyrics → manifest) completes before the next item starts. Two mechanisms enforce this together: (a) the `process_queue()` cascade lives inside the completion task so a follow-up item is *requested* only after the current one finishes (#455); (b) the queue *slot* (`active_count`) is held throughout post-processing — `q.on_task_finished()` is called at the bottom of the completion task (right after `set_complete`), not at primary GAMDL exit, so any concurrent `process_queue` invocation (user IPC, fallback retry, startup recovery) sees `active_count == max_concurrent` and is blocked from grabbing the next item early (#706 — pre-fix the slot decremented at line 6246 and items legitimately ran in parallel during post-processing). The completion task takes ownership of an `ActiveSlotGuard` RAII guard at spawn; if the task panics or is aborted before the explicit release, the guard's `Drop` fires a fire-and-forget `tokio::spawn` to release the slot, preventing a permanent queue stall. Happy path calls `guard.disarm()` after the explicit `q.on_task_finished()` so Drop is a no-op. Completion task has a scaled timeout: `compute_completion_timeout(track_count)` for the enrichment branch (10 min base + 10 s/track, capped at 4 h, #461 / #579), and `compute_companion_timeout(track_count, tier_count)` for the companion-wait branch (enrichment budget + 8 min/tier, same cap, #705) — multi-tier modes (e.g. Atmos→all formats = 4 full GAMDL re-downloads) need the per-tier headroom. Soft timeout warns and waits a second deadline before hard-aborting via `CompanionTaskHandle::abort()`. Hard / soft / enrichment activity-log messages identify the affected item via `format_content_label(&QueueItemStatus)` (Artist — Album — Track, URL fallback). `set_complete()` clears `processing_label`, `speed`, and `eta` fields (#416).
- **Dual API metadata enrichment** (#454): iTunes Lookup API (no auth, public) runs FIRST as Step 0, then Apple Music Catalog API (MusicKit JWT) overwrites with richer data in Step 1. iTunes provides: Country, DiscCount, iTunesTrackURL. Price/currency excluded from file tags (locale-dependent). Apple Music provides: editorialNotes, audioTraits, animated artwork, contentRating, lastModifiedDate, etc. Tracks matched by track/disc number.
- **MetadataProvider trait** (#351): Service-agnostic async trait in `metadata_provider.rs` with `ProviderPriority` enum (Supplementary=0 / Primary=1 / UserOverride=2). `MetadataProviderRegistry` stores providers sorted by priority. Enables multi-service metadata fetching through a common interface.
- **Cover art naming** (#448): `CoverArtName` enum (FrontCover, Cover, Folder) in `settings.rs` with `cover_art_name` setting (default: FrontCover). `rename_cover_art()` in `download_queue.rs` renames GAMDL's hardcoded `Cover.<ext>` to user's configured stem. Idempotent (skips if target exists or source absent). Dropdown in Settings > Cover Art.
- **Input validation** (#459): `validate_path_safe()` in `config_service.rs` rejects `..` path traversal sequences. `start_download()` validates URL domains (Apple Music/Classical/iTunes only). `settings.json` file permissions set to `0o600` on Unix.
- **Wrapper auth logging**: At download start, a non-verbose `emit_download_log()` entry shows the active authentication method: "Authentication: Wrapper ({redacted_url})" or "Authentication: Cookie-based (no wrapper)".
- **Smart re-download detection**: When `smart_redownload_detection` is enabled (default: `true`), `DownloadForm.tsx` checks the download history before queueing a URL. If the URL was previously downloaded, shows an info toast with the original download date. The Apple Music API's `lastModifiedDate` field is extracted during enrichment and stored in the `manifest.meedyadl` manifest (`ManifestSource.last_modified_date`) and as `AlbumLastModified`/`AppleLastModifiedDate` atoms in M4A files. Detects: audio quality upgrades, tracks added, metadata corrections, Apple Digital Master certification. Cannot detect: silent audio remasters without metadata changes. Toggle in Settings > General > Preferences. IPC: `check_redownload_status(url)` in `commands/gamdl.rs`.
- **Tool version tracking** (#273): `get_tool_version()` in `dependency_manager.rs` runs binaries with `--version` flag and extracts semver from output. Per-tool parsing logic (FFmpeg, MP4Box, mp4decrypt, N_m3u8DL-RE). Auto-update checking via GitHub API compares installed vs latest available versions. `tool-versions.toml` defines minimum supported versions per tool.
- **Functional tool verification** (#391): `check_all_dependencies()` at startup runs each tool binary with `--version` (2-second timeout) instead of just checking file existence. Detects corrupted or non-executable binaries. If binary fails to spawn, reported as not installed; if it spawns but `--version` fails, still reported as installed.
- **Browse button in setup wizard** (#278): "Browse" button in `DependenciesStep` next to "Install" for each missing tool. Opens native file picker via Tauri dialog plugin to locate existing binary. Selected path stored in tool-specific settings so app uses user's existing installation instead of downloading.
- **Rollback UI for pre-release users** (#267): `UpdateCheckResult` includes `rollback_version` and `rollback_tag` fields. When running a pre-release build, the latest stable release is offered as a rollback option in the Updates page. Users can downgrade to stable via the in-app updater.
- **Platform template variable** (#309): `{platform}` available in GAMDL filename/folder templates (e.g., `{album_artist}/{album}/{platform}`). Resolves to download platform name (e.g., "AppleMusic", "Spotify"). Defined in `TEMPLATE_VARIABLES` in `template-parser.ts`.
- **ARIA accessibility** (#182): `aria-label` + `aria-describedby` on DownloadForm URL textarea, `role="status"` + `aria-live="polite"` on StatusBar for screen reader support. Joins 55+ existing ARIA attributes across components (FallbackChainList, ProgressBar, Toggle, SettingsSection, etc.).
- **Frontend tests** (#232): Vitest component tests for activity and download stores. Multi-service URL detection tests for `url-parser.ts`. Unit tests for `find_album_directory`, `rename_cover_art`, `validate_path_safe` (#460).
- **meedya-core migration status** (#352, #353): **Phases 1+2+3 of #353 implemented** — `acoustid_service` and `replaygain_service` now consume the shared `meedya-fingerprint` crate from `MWBMPartners/MeedyaSuite-core` for both the AcoustID HTTP client (#353 Phase 1, commit `85dfb69f`) and the ReplayGain ebur128 analyser (#353 Phase 2, commit `220b6c7a`). Chromaprint fingerprint generation moved upstream behind a `chromaprint` cargo feature flag (#353 Phase 3, MeedyaSuite-core PR #35; consumer-side commit `19d7f2e6` drops MeedyaDL's local `rusty-chromaprint` + `symphonia` deps). Three thin adapter functions `map_shared_acoustid_result` / `map_shared_replaygain_result` / `map_shared_chromaprint_result` project the shared `FingerprintError` variants back to MeedyaDL's historical `String` error surface so existing log-scrapers and call-sites stay aligned. **Lyricsfile (`.lyrics`) YAML format** (#596) consumes the new `meedya-lyrics::Lyricsfile` module from MeedyaSuite-core PR #36 the same way (consumer-side commit `102c1f51`). When MeedyaSuite-core PRs #35 and #36 merge to main, MeedyaDL's `Cargo.toml` flips the two `branch = "feat/..."` deps back to `branch = "main"` in a single-line follow-up PR.

## Key Directories

```text
src-tauri/src/          # Rust backend
  commands/             # IPC command handlers (system, dependencies, settings, gamdl, credentials [includes validate_musickit_credentials, has_webplayer_token, clear_webplayer_token, check_dev_access, activate_dev_access, deactivate_dev_access], updates, cookies, login_window, artwork, crash_reports [includes get_github_issue_url], api_audit, history, clipboard)
  models/               # Data structures (download, settings, gamdl_options, dependency, media_service, crash_report, codec_registry, tag_registry, manifest, content_match, service_status, votify_options, ytdlp_options, get_iplayer_options)
  services/             # Business logic (python_manager, gamdl_service, dependency_manager [4 required tools], config_service, download_queue, update_checker, cookie_service, login_window_service, animated_artwork_service, apple_music_api, metadata_tag_service, metadata_provider, acoustid_service, replaygain_service, enhanced_lyrics_service, crash_report_service, webvtt_service, rich_srt_service, musicbrainz_service, history_service, health_check_service, api_audit_service, ass_subtitle_service, mediainfo_service, pip_engine_service, clipboard_service, engine_registry, engine_runner, smart_download, service_status, bpm_service, integration_tests)
  utils/                # Platform, archive, process, activity_log utilities
src/                    # React frontend
  components/           # UI components (common, layout, download, settings [includes CrashReportSection, CrashReportDialog, DevToolsSection], setup, help, updates)
  hooks/                # React hooks (usePlatform, useTheme, useKeyboardShortcuts, useKonamiCode, useClipboardMonitor)
  stores/               # Zustand state stores (ui, settings, download, activity, dependency, setup, update)
  lib/                  # Utilities (tauri-commands, url-parser, quality-chains, i18n, template-parser)
  types/                # TypeScript type definitions mirroring Rust models (includes codec-registry.ts)
  styles/themes/        # Platform CSS (base, macos, windows, linux, a11y-high-contrast, a11y-colour-blind)
public/locales/         # i18n translation files ({lang}/translation.json)
public/                 # Web runtime assets (logo.svg, logotype.svg, app-icon.svg, favicon.ico)
help/                   # Markdown help documentation (12 topics: index, getting-started, downloading-music, downloading-videos, lyrics-and-metadata, quality-settings, fallback-quality, cookie-management, animated-artwork, keyboard-shortcuts, faq, troubleshooting)
scripts/                # Build utilities (copyright year updater, version bump, icon generator, APNG generator)
assets/brand/           # Brand assets: SVGs, animated PNGs, icons (ICO/ICNS/PNG), brandkit.html. PROPRIETARY LICENSE.
.github/ISSUE_TEMPLATE/ # Issue templates (crash-report.yml)
.github/workflows/      # CI, Release, Release Please, Changelog, CodeQL, Dependency Report, Fix Updater Manifest workflows (7 total)
```

## Implementation Phases (All Complete)

- **Phase 1**: Project foundation - scaffold, config, CI/CD, docs
- **Phase 2**: Core backend - Python manager, GAMDL service, dependency manager, settings, credentials
- **Phase 3**: Core UI - Zustand stores, layout, download form, settings pages, setup wizard, help viewer
- **Phase 4**: Download system - Queue manager, fallback quality chain, progress tracking, retry/clear
- **Phase 5**: Advanced features - Cookie import, auto-updates, help search, system tray, service architecture
- **Phase 6**: Polish & release - Icons, CI fixes, testing, docs, release workflow, release-please integration
- **Phase 7** (#107): Multi-service architecture foundation — MediaServiceId enum, EngineRegistry, EngineCommandBuilder trait, service-aware queue, per-service settings, engine fallback, help docs

## Conventions

- **Copyright header**: Every source file starts with `// Copyright (c) 2026 MeedyaSuite` + MIT license reference
- **Comments**: Every function and significant code block gets detailed comments
- **Conventional commits**: Required for automated changelog generation (release-please)
- **`Release-Note:` trailers apply to direct-pushed commits too (#1046)**: This project's "push `fix:`/`feat:` commits directly to main" convention (see Release Workflow below) bypasses the PR-only `release-note-gate.yml` trailer check entirely. Direct-pushed `feat`/`fix`/`perf` commits MUST end their body with `Release-Note: <plain English line>` trailer line(s) (or `Release-Note: none` when nothing is user-visible), exactly like PR bodies — see `.github/release-notes/STYLE_GUIDE.md`. `release-note-gate.yml`'s `push-trailer-advisory` job scans every push to `main`/`alpha`/`beta`/`release-candidate` and posts a `::warning` (non-blocking — a push can't be un-pushed) for any feat/fix/perf commit missing one, but the habit matters more than the warning: a missing trailer means that commit's user-facing change is silently absent from every ELI5 release body it lands in.
- **GAMDL options**: All 11 audio codecs, 8 video resolutions, all CLI flags typed as Rust enums in `models/gamdl_options.rs`. Includes `song_codec_priority` (for GAMDL >= 2.9.1 native multi-codec support) and `artist_auto_select` (7 variants: main-albums, compilation-albums, live-albums, singles-eps, all-albums, top-songs, music-videos). **Multi-select artist auto-select**: `artist_auto_select_multi: Vec<ArtistAutoSelect>` stores multiple modes; when downloading from an artist URL with multiple modes selected, `start_download` creates N separate queue items (one per mode) since GAMDL only accepts a single `--artist-auto-select` value. Legacy `artist_auto_select` field kept in sync for backwards compatibility.
- **Version-aware GAMDL dispatch** (supports **GAMDL v3.x only — `>= 3.0`; v2 support dropped 2026-07-03**, ceiling 3.8.4; v3.0–v3.5.x use wrapper-v1, v3.6+ use wrapper-v2, both supported): `services/gamdl_capabilities.rs` exposes a process-global cache of the detected GAMDL version string (via `RwLock<Option<String>>`) and a `supports(GamdlFeature)` helper. The cache is populated from `gamdl_service::get_gamdl_version()` (called by the startup dependency probe in `commands::dependencies::get_component_versions` and by the queue on first download) and from `gamdl_service::install_gamdl()` on every `pip install --upgrade`. Current gates: (a) `GamdlFeature::FetchExtraTags` — `--fetch-extra-tags` / `fetch_extra_tags` INI key, available on v2.x only (GAMDL v3.0 removed it in upstream commit `61ea24b`, "Remove extra tags fetching and preview parsing"); (b) `GamdlFeature::NativeCodecPriority` — native `--song-codec-priority` album support, available on >= v2.9.1; (c) `GamdlFeature::PlaylistFolderTemplate` — `--playlist-folder-template` CLI flag and `playlist_folder_template` INI key, available on >= v3.0 (#618); (d) `GamdlFeature::WrapperM3u8Ip` — v3.1+ (#605); (e) `GamdlFeature::NoExceptionsFlag` — three-era predicate: effective on `< 3.1` (original), no-op on `3.1..3.7.4` (upstream `dc6f2e8` removed every consumer of the flag), **effective again on `>= 3.8`** (upstream `58f4548` gated the `structlog` `ExceptionPrettyPrinter` on `not config.no_exceptions`, restoring the suppression). Consumed by `download_queue::merge_options` which routes the version gate through the capability enum as the single source of truth — the pre-3.1 and `>= 3.8` eras both let the flag through to `to_cli_args()`; the 3.1..3.7.4 window and the unknown-version case both drop it. Same three-era shape as `FFmpegPath`. Consumers: `merge_options()` in `download_queue.rs` leaves `options.fetch_extra_tags` as `None` on v3.0 (so `to_cli_args()` never emits the removed flag), `ini_metadata_section()` in `config_service.rs` skips the `fetch_extra_tags = true` INI line, and both `GamdlOptions::to_cli_args` and `config_service::ini_template_section` gate the `playlist_folder_template` emission behind the v3.0+ capability. GAMDL v3.0's INI loader silently drops unknown keys via `cleanup_unknown_params()` so stale v2.x entries are harmless, but we still gate writes to keep emitted files self-consistent with the detected CLI. Unknown-version default is `false` for every feature (we never emit options the subprocess might reject). GAMDL v3.0 also migrated logging to `structlog`; `ERROR_PREFIX_REGEX` in `utils/process.rs` accepts an optional `[LEVEL    HH:MM:SS]` prefix so `parse_gamdl_output()` still classifies wrapped `Error ...` lines as `GamdlOutputEvent::Error` instead of demoting them to `Unknown`. **GAMDL v3.2** (2026-04-24) flipped `AppleMusicInterface.concurrency` default from 5 → 1, making album/playlist metadata fetch sequential by default — an upstream reliability fix (fewer AMP API rate-limits / cascading failures) that MeedyaDL picks up for free the moment a user upgrades; no CLI flag was added so we can't tune it. Also narrowed the conditions under which `track_log.info('Downloading ...')` is emitted (now `partial AND media_type in {songs, library-songs, music-videos, library-music-videos, uploaded-videos}`) — MeedyaDL's `TRACK_INFO_V2_REGEX` is unaffected because it matches the bracketed `[Track N/M]` shape rather than the `media_type` directly. See `.github/audits/gamdl-v3.2-audit.md` for the full cross-version diff and the set of audit-derived issues (#613–#620). **GAMDL v3.3** (2026-04-24, same day) is a one-commit playlist bugfix (`c83e47d`, "Remove total arg from media fetch calls") that drops a stale `total=...` kwarg from two `_get_*_media` invocations inside `interface.py::_get_playlist_media` — zero MeedyaDL-facing surface changes (no CLI / INI / output / regex deltas), so the support window admits 3.3 with no `GamdlFeature` gate adjustments needed. **GAMDL v3.4** (2026-04-27) swapped GAMDL's logging output stream from stderr → stdout (`logging.StreamHandler()` → `structlog.PrintLoggerFactory(file=CustomOutputWriter([sys.stdout]))`) and enriched the subprocess-failure message format from `'"<cmd>" exited with code N'` to `'Exited with code N: <args>\nstdout:\n…\nstderr:\n…'`. Both changes are benign — the stdout/stderr reader tasks in `download_queue.rs` parse identically via `parse_gamdl_output()`, the substring-based `process::classify_error()` actually classifies **better** with the new richer subprocess error text, and the latent cosmetic `──── [Track N/M] Downloading ────` separator (only emitted from the stdout reader) starts firing reliably from 3.4+ as a free UX improvement (it was a no-op pre-3.4 because TrackInfo lines came from stderr). The release also added `Database(overwrite)`, fixed a `DownloadItem` UUID-sharing bug, refactored cleanup gating, stripped a sized-image suffix from raw cover URLs, and reworded `GamdlInterfaceFlatFilterExcludedError` — all internal to GAMDL or scoped to the `--database-path` codepath we don't expose. **GAMDL v3.5** (2026-04-27, same day) is a 4-commit fix to GAMDL's iTunes lookup HTTP layer for music-video metadata: `httpx.AsyncClient(follow_redirects=True)`, `X-Apple-Store-Front` header `-1` suffix removed, and `storefront_id=None` for non-US storefronts. Pure upstream win — better music-video coverage, especially for non-US users. See `.github/audits/gamdl-v3.4-v3.5-audit.md` for the full cross-version diff; no `GamdlFeature` gates added or re-thresholded. **GAMDL v3.5.1** (#711, audit `.github/audits/gamdl-v3.5.1-audit.md`, 2026-05-07) is a one-commit music-video m3u8 URL HTTP 403 fix (upstream `34a397e`); zero MeedyaDL surface change. **GAMDL v3.5.2** (#767, audit `.github/audits/gamdl-v3.5.2-audit.md`, 2026-05-13) is a four-commit bug-fix release: (a) `_amp_request` pagination now forwards all `next_params` except `limit` (fixes silent pagination failures on Apple Music resources whose `next` link carries query params beyond `offset` — playlists with editorial art, artist track lists with `extend` / `include`); (b) `gamdl/interface/music_video.py` rewrites `play-edge.itunes.apple.com` → `play.itunes.apple.com` (was rewriting `itunes.apple.com`) to track Apple's edge-host migration of the music-video m3u8 master playlist — the user-facing fix referenced in the release title; silently unblocks MV downloads that broke on 3.5.1 once Apple flipped the host; (c) `CustomOutputWriter.add_file()` opens log files as UTF-8 (only matters for `--log-file`, which MeedyaDL never passes — we capture stdout/stderr via `BufReader::lines()`); (d) a `log.debug()` line gated behind `--log-level DEBUG`, which MeedyaDL also never sets (`GamdlOptions.log_level` is defined in the option struct but unwired to settings). Zero CLI / INI / output-format / regex / log surface change at the default INFO log level; no `GamdlFeature` gate touched. Same zero-code-change admission shape as v3.3 (playlist fix), v3.5 (iTunes lookup fix), and v3.5.1 (music-video 403 fix). **GAMDL v3.7.2 + v3.7.3** (#898, audit `.github/audits/gamdl-v3.7.2-v3.7.3-audit.md`, 2026-05-28) shipped the same day as a pair of bug-fix patches (3 functional commits, ~4 lines changed across both). (a) `interface/music_video.py::get_tags` swaps music-video `title` / `album` tags from the *censored* variants (`trackCensoredName` / `collectionCensoredName`) to the *uncensored* ones (`trackName` / `collectionName`), and routes the censored variants into the standard sort-order atoms `sonm` (title sort) and `soal` (album sort). MeedyaDL writes neither sort atom, so we don't collide — the only user-visible effect is that MV filename templates containing `{title}` or `{album}` produce uncensored filenames after upgrade. Songs are unaffected. (b) Defensive `.get("playParams", {}).get("isLibrary")` access in both `song.py::get_media` (3.7.2) and `music_video.py::get_media` (3.7.3) prevents bare `KeyError: 'playParams'` tracebacks for unstreamable / library-only items. Affected songs / MVs now reach the existing `is_media_streamable` check and surface `GamdlInterfaceMediaNotStreamableError("Media is not streamable: <id>")` reliably. The error class itself predates 3.5.2; what's new is that this message now reaches users instead of being masked by the KeyError. MeedyaDL change: new `is_media_not_streamable_error()` helper + dedicated `media_not_streamable` classifier bucket in `utils/process.rs`, ordered before the generic `not_found` substring fallback so the actionable "removed / region-locked / library-only" guidance wins over the broader "content not found" message (6 unit tests in `process::tests`). No `GamdlFeature` gate, no new CLI/INI/wrapper-protocol surface. **GAMDL v3.7.4** (#925, audit `.github/audits/gamdl-v3.7.4-audit.md`, 2026-06-12) is a 5-commit upstream reliability patch (4 functional + 1 version bump). (a) `interface/song.py::_switch_m3u8_master_url_to_default` regex-rewrites HLS variant master URLs to the canonical `_default.m3u8` form across both `_get_m3u8_from_playback` and `_get_m3u8_master_url_from_metadata` — internal to GAMDL's HLS pipeline; MeedyaDL never inspects m3u8 URLs, so the only repository references are *comments* describing GAMDL's debug-log key. (b) `api/apple_music.py` token-extraction regex updated for Apple's `index-legacy-*.js` → `index-*.js` home-page bundle rename + JWT capture tightened to require structurally valid three-segment tokens (`eyJ*.eyJ*.*`) inside quotes — fixes GAMDL's own `get_token()`; MeedyaDL's `apple_music_api.rs::TokenSource` three-tier chain (user MusicKit JWT > embedded > web-player keychain) is independent. (c) `interface/base.py::get_cover_bytes` now uses an explicit 30s timeout + `follow_redirects=True` on the httpx client — reliability win for cover-art HTTP fetches; MeedyaDL observes the resulting cover-image *files* on disk, not the HTTP path, so MeedyaDL's own static cover-art fallback chain (#756) is unaffected. (d) `api/exceptions.py::GamdlApiResponseError.content` typed `Any | None` (was `str | None`); non-string content now JSON-serialised via `json.dumps()` with `TypeError` fallback to `str()`. The `Resource Not Found` literal that `process::is_storefront_mismatch_error` keys on is preserved, and `PYTHON_EXCEPTION_REGEX` keys on the class-name prefix in the traceback not on the content payload — both verified compatible. Same zero-code-change admission shape as v3.3 (playlist fix), v3.5 (iTunes lookup fix), v3.5.1 (music-video 403 fix), v3.5.2 (pagination + edge-host fix), and v3.7.1 (refactor-only); no `GamdlFeature` gate touched. **GAMDL v3.8** (#962, audit `.github/audits/gamdl-v3.8-audit.md`, 2026-06-29) is a seven-commit release (six functional + one version bump) that breaks the recent zero-code-change admission streak. The headline behavioural change is upstream commit `a7d141b7`: a new `POST /v1/play/assets` HLS endpoint replaces the song-metadata `extendedAssetUrls.enhancedHls` read on GAMDL's non-wrapper code path. This unlocks every non-web codec except ALAC for wrapper-less downloads (aac / aac-he / aac-binaural / aac-downmix / aac-he-binaural / aac-he-downmix / atmos / ac3). Companion commit `4d2988b3` narrows GAMDL's own CLI warning + README wording to say only ALAC is now wrapper-dependent. MeedyaDL doesn't call `/v1/play/*` itself and doesn't parse m3u8 URLs — the HTTP / auth / INI / template / regex / classifier surfaces are all zero-touch — but `SongCodec::is_wrapper_dependent()` (in `models/gamdl_options.rs`, still returning `true` for Atmos + AC3) and the `(Experimental)` labels on the codec dropdown (`display_name()`) are conceptually stale on 3.8+; both filed as `for consideration` follow-ups, no correctness regression. Commit `58f4548` restored `--no-exceptions` effectiveness (silently a no-op on 3.1..3.7.4 because `structlog.processors.ExceptionPrettyPrinter()` was in the processor list unconditionally); 3.8 gates it on `not config.no_exceptions`. **MeedyaDL fix required**: `merge_options` at `download_queue.rs:3556-3560` used to zero the field on any 3.1+ version via a direct `is_version_at_least("3.1")` check, silently denying 3.8 users the restored suppression — the direct check has been replaced with the `GamdlFeature::NoExceptionsFlag` gate (the capability enum is now the single source of truth), and the gate itself gained a three-era predicate matching the `FFmpegPath` shape (effective on `< 3.1` and `>= 3.8`, no-op on the 3.1..3.7.4 window). Downstream `is_python_traceback_noise` filter (#660) already suppresses the noise — UX / defence-in-depth, not urgent. Commits `d26b895b` (uploaded-video interface bug fixes — three broken references), `4e97b0c4` + `8e3b94f6` (`--use-album-date` now honoured on songs, with `releaseDate` fallback for missing `playlistId`), and `6cdbe015` (version bump) admit at zero MeedyaDL surface cost. Same overall shape as the v3.7.2 + v3.7.3 audit (#898) — bug-fix-heavy release with one targeted code change; not a pure ceiling bump like v3.3 / v3.5 / v3.5.1 / v3.5.2 / v3.7.4. **GAMDL v3.8.1** (audit `.github/audits/gamdl-v3.8.1-audit.md`, 2026-07-03) is a **zero-code-change admission** — one functional commit (`30f02156`, "Support non-enhanced song HLS playlists") + version bump, fixing an upstream issue that prevented some songs from downloading with non-web codecs (follow-up to v3.8's `/v1/play/assets` change). `30f02156` touches only `interface/song.py` (renames private `_get_stream_info` → `_get_stream_info_nonweb`; adds private `_is_enhanced_m3u8_master` / `_get_stream_info_enhanced` / `_get_stream_info_nonenhanced` / `_get_playlist_from_codec_*` helpers — all internal to GAMDL's HLS stream-info selection) and `cli/interactive_prompts.py` (`ask_song_codec` label helper, only reachable in GAMDL's interactive prompt which MeedyaDL never invokes). No CLI/INI/exception/output-shape change; the only added logs are DEBUG-level `logger.bind(action=…)` context binds. MeedyaDL never parses m3u8/stream-info, so zero surface impact — a free wrapper-less non-web-codec reliability win. **Shipped alongside the v2-support drop**: `minimum_version` 2.9.1 → 3.0 in `tool-versions.toml`. The supported line is now v3.x only — v3.0–v3.5.x on wrapper-v1 (three sockets), v3.6+ on wrapper-v2 (`--wrapper-url`), both fully supported. No `GamdlFeature` predicate changes: the v2.9.1-keyed gates (NativeCodecPriority / ClassicalMusicHostRequired / StorefrontIniKeyStripped) go always-true inside `[3.0, 3.8.1]` but keep their version-math; `FetchExtraTags` (v2.x-only) goes permanently inert (emission already suppressed) — removing the dead `fetch_extra_tags` plumbing is a filed `for consideration` follow-up. No test changes needed (`classify_*` tests read the window dynamically; gate tests are pure math) — `cargo test services::gamdl_capabilities` → 32/32. **GAMDL v3.8.2** (#1009, audit `.github/audits/gamdl-v3.8.2-audit.md`, 2026-07-10) is **ADMITTED** (ceiling 3.8.1 → 3.8.2). It replaced Python `amdecrypt.py` with a compiled Rust `ammuxer` extension, published as `cp310-abi3` wheels (stable ABI, load on CPython 3.10+ incl. the bundled 3.12) for macOS universal2 / Windows x64+ARM64 / Linux x64+aarch64 — installable on 5 of 6 platforms (only Linux ARMv7 lacks a wheel; the `gamdl>=3.0,<=3.8.2` + `--only-binary=gamdl` spec auto-falls-back to 3.8.1 there). Verified genuinely-abi3 by importing `gamdl._ammuxer` + running `python -m gamdl --version` on CPython 3.14. **Wheel-availability is a first-class audit gate** (an initial pass wrongly HELD at 3.8.1 on stale/incomplete PyPI data — always re-check the wheel set). The one real MeedyaDL code change: 3.8.2 moved wrapper-v2 decrypt from HTTP `POST /decrypt` to a native TCP host/port (`--wrapper-decrypt-host`/`--wrapper-decrypt-port`), so new `GamdlFeature::WrapperDecryptHostPort` (>= 3.8.2) emits those from the existing `wrapper_decrypt_ip` setting (CLI in `gamdl_options.rs` + INI in `config_service.rs`, split at the last `:`), the wrapper-v2 TCP decrypt preflight now runs on 3.8.2+, and a `/me` version preflight warns when the daemon != 0.0.2 (3.8.2 exact-matches it at startup). `no_compatible_wheel` (`update_checker.rs`) is platform-aware (flags only ARMv7). A live smoke-test (real download + wrapper-v2 0.0.2 round-trip on each platform) is the pre-stable gate. **GAMDL v3.8.3 + v3.8.4** (#1018, audit `.github/audits/gamdl-v3.8.3-v3.8.4-audit.md`, 2026-07-18) are a **zero-code-change admission pair** (ceiling 3.8.2 → 3.8.4). The 14-commit `3.8.2..3.8.4` range contains zero Python-source changes (only the `__version__` string): 3.8.3 (2026-07-12) is the pyo3 0.23.5→0.27.2 bump for Python-3.14 sdist-build support (mechanical `py.allow_threads`→`py.detach` / `downcast`→`cast` renames inside the private Rust `_ammuxer` extension; `abi3-py310` unchanged, still loads on the bundled 3.12 and reused system Python >= 3.10) plus the CI work that formalised the cross-platform abi3 wheel publishing; 3.8.4 (2026-07-16) is one Rust commit (`e4887d34`, `ammuxer/src/media.rs`): a `flush_then_write_immediate()` helper in `decrypt_track_wrapper()` that flushes queued batched samples before immediate writes so payload order matches the `stsz` table — **fixing corrupted/truncated song endings on wrapper-decrypt downloads, a data-corruption bug present in 3.8.2/3.8.3** (extra urgency for the bump; the pre-stable live smoke-test is retargeted at 3.8.4 with a song-ending integrity check). `gamdl/api/wrapper.py` is untouched, so `TARGET_WRAPPER_API_VERSION` stays 0.0.2 and MeedyaDL's `/me` preflight + `wrapper_version_mismatch` guidance remain correct; every 3.8.2-keyed surface inherits via `GamdlFeature::WrapperDecryptHostPort` (`>= 3.8.2`). Wheels re-verified live (first-class gate): 3.8.3 and 3.8.4 ship identical `cp310-abi3` sets for macOS universal2 / Win x64+ARM64 / Linux x64+aarch64 — still no ARMv7 wheel, so `gamdl>=3.0,<=3.8.4` + `--only-binary=gamdl` resolves 3.8.4 on 5 of 6 platforms and ARMv7 keeps falling back to 3.8.1. Same zero-code-change shape as v3.3 / v3.5 / v3.5.1 / v3.5.2 / v3.7.4 / v3.8.1.
- **`song_codec` / `song_codec_priority` emission** (#614, #617): the `--song-codec` flag was removed from GAMDL in the v2.9.1 CLI restructure and does not exist in any release in our support window; `audio_cli_args()` in `gamdl_options.rs` always emits `--song-codec-priority` (promoting a scalar `song_codec` to a one-element CSV when the explicit priority chain is unset). The companion `ini_audio_section()` in `config_service.rs` is intentionally empty — `song_codec` is not a valid key, and `song_codec_priority` is dropped by `cleanup_unknown_params()` because upstream's dataclass field is misspelled `song_codec_piority` (`dataclass_click` propagates the Python field name to `click.Parameter.name`, which is what GAMDL keys INI lookups against). Codec preference reaches GAMDL exclusively via the CLI flag path on every supported release.
- **Abort-all queue action** (#620): `DownloadQueue::abort_all()` transitions every non-terminal item to `Cancelled` and returns an `AbortSummary` with per-pre-state counts. Surfaced as the `abort_all_downloads` IPC + `abortAllDownloads()` frontend wrapper + red "Abort Queue" button with confirmation modal in `DownloadQueue.tsx`. Rides on the existing cancellation-poll loop to reap subprocesses (already `kill_on_drop(true)`). Terminal items (`Complete` / `Cancelled` / `Error`) are left untouched so history stays intact. Emits a `downloads-aborted` event + single summary toast + `[System]` activity-log entry. Distinct from `clear_all` (which only removes non-active items) and per-item `cancel_download` (which is polling-latency-limited).
- **GAMDL support window**: Each MeedyaDL build declares a validated GAMDL version range via `tool-versions.toml` → `[gamdl]` (`minimum_version`, `maximum_tested_version`, `recommended_version`). `services/gamdl_capabilities.rs` exposes `support_window()`, `classify(installed)` → `VersionSupport::{NotInstalled, Supported, Unsupported, Untested}`, `should_offer_upgrade(candidate)`, `is_above_tested_ceiling(candidate)`, `pip_version_spec()`, and `pip_target_spec(target)`. Enforcement: `gamdl_service::install_gamdl(app, target_version)` runs `pip install --upgrade 'gamdl>={min},<={max}'` when `target_version` is `None` (routine setup-wizard / "Upgrade tested" path) and `pip install --upgrade 'gamdl=={target}'` when an explicit target is supplied (the user opted into an above-ceiling "Untested" upgrade). `commands::dependencies::log_component_versions_to_activity` emits a `[System]` activity-log entry on startup classifying the installed GAMDL version against the support window (`log::warn!` for Unsupported/Untested, `log::info!` for Supported/NotInstalled). The canonical, user-facing matrix lives in README.md; SECURITY.md references it. Bumping the ceiling is a one-file PR: edit `maximum_tested_version` in `tool-versions.toml` and ship a new MeedyaDL release.
- **Above-ceiling GAMDL updates surface as "Untested"** (audit branch: `claude/fix-meedyadl-gamdl-updates-ucJWJ`): Previously `should_offer_upgrade` capped at `maximum_tested_version`, which silently hid every newer upstream release behind an "All components up to date" status — operators had no signal that a new GAMDL build was waiting for validation. The fix splits the two concerns: `should_offer_upgrade` now only rejects unparseable strings (so users see the new version), and a new `ComponentUpdate.is_untested: bool` flag — driven by `gamdl_capabilities::is_above_tested_ceiling` — carries the warning state to the UI. `UpdateBanner.tsx` and `UpdatesPage.tsx` render an amber "Untested" badge plus a disclaimer paragraph for above-ceiling GAMDL releases. The Upgrade button on those rows passes the explicit `latest_version` through `upgradeGamdl(targetVersion)` → `upgrade_gamdl` IPC → `install_gamdl(app, Some(target))` → `pip_target_spec` (`gamdl=={target}`), so pip lands on exactly the version the banner advertised instead of silently resolving down to the bounded ceiling. Routine "Upgrade tested" clicks still go through `pip_version_spec` so an unaudited release can never sneak in via a future-version `--upgrade` resolution. **GAMDL v3.3** (2026-04-24) audit landed alongside this fix: the entire delta from 3.2 is a single internal commit (`c83e47d`, "Remove total arg from media fetch calls") that strips a stale `total=...` kwarg from two `_get_*_media` calls inside `interface.py::_get_playlist_media` — pure playlist-download bugfix, zero CLI / INI / output / regex surface changes. `maximum_tested_version` and `recommended_version` were both bumped to `3.3` in the same patch.
- **Fallback quality chains**: Version-aware strategy with safety net. For GAMDL >= 2.9.1: first attempt uses native `--song-codec-priority` flag to pass the entire fallback chain (e.g., `atmos,alac,aac,...`) in a single process invocation. If native priority fails, MeedyaDL's own `try_fallback` system kicks in as a safety net, retrying each codec individually via `--song-codec-priority <single-codec>`. For GAMDL < 2.9.1: MeedyaDL's `try_fallback` is the only mechanism. `try_fallback()` returns `Option<(GamdlOptions, usize, usize)>` — the new options, 1-indexed fallback position, and chain length. Activity log shows: "Format not available — trying {codec} (fallback {n} of {total})". Music chain: ALAC→Atmos→AC3→AacBinaural→Aac→AacLegacy; Video: 2160p→...→240p. Config.ini dual-key strategy writes both `song_codec` (for < 2.9.1) and `song_codec_priority` (for >= 2.9.1) to ensure compatibility across GAMDL versions. **Gap-fill retry**: When native priority partially succeeds (some tracks downloaded, some skipped due to experimental codecs without wrapper), MeedyaDL automatically re-runs GAMDL with `overwrite=false` and a filtered priority chain excluding wrapper-dependent codecs (Atmos, AC3). This fills gaps with lossless/lossy fallbacks without overwriting successful experimental-codec downloads. Helpers: `count_codec_skip_warnings()`, `build_gapfill_priority_chain()`, `count_audio_files_in_directory()`. `SongCodec::is_wrapper_dependent()` identifies codecs requiring wrapper auth. `SongCodec::from_cli_string()` parses CLI strings back to enum variants.
- **Apple Music URL support**: Three domains accepted: `music.apple.com` (standard), `classical.apple.com` (Apple Music Classical), and `itunes.apple.com` (legacy). Validated in both the frontend URL parser (`src/lib/url-parser.ts`) and backend regex patterns in the Rust services. All five `parse_apple_music_url` entity regexes in `apple_music_api.rs` alternate `(?:classical|music|itunes)` consistently (album, song, music-video, artist, catalog playlist) so iTunes URLs ride the same metadata-prefetch and storefront-normalisation rails as the other two domains (#548). **Non-geographic URLs** (missing storefront code, e.g., `music.apple.com/album/...`) are auto-normalized at enqueue time by injecting a storefront based on OS locale (or `"us"` fallback) via `normalize_apple_music_url()` in `apple_music_api.rs`. GAMDL requires the storefront for its URL regex but ignores it for API calls (uses cookies/wrapper auth). Enrichment API calls use `fetch_album_metadata_with_fallback()` which retries with alternative storefronts (OS locale, `"us"`) when the primary returns 404 — handles region mismatches from shared links. **Personal library URLs** (`/library/albums/l.XXXX`, `/library/songs/...`) are accepted as `'library'` content type by the frontend URL parser and passed through to GAMDL as-is.
- **URL audit diagnostics (#487 umbrella — #546/#547/#548/#549)**: `start_download` in `commands/gamdl.rs` emits classification logs for four URL classes with known quirks, so downstream filename-path behaviour can be correlated without replaying the download. (a) `/library/` URLs log at INFO noting no MeedyaDL safety net applies (#546). (b) `classical.apple.com` URLs log at INFO noting shared templates with `music.apple.com` and the movement-title collision risk when album context is lost (#547). (c) `itunes.apple.com` URLs log at WARN — the parser regex gap is closed but GAMDL's own URL-regex compatibility with the legacy domain is still unverified (#548). (d) Any URL that passes the host allowlist but returns `None` from `parse_apple_music_url` after normalisation (and isn't a library URL) logs at WARN as "Unrecognised Apple Music URL shape" — catches uploaded / post / novel path variants without hard-coding specific substrings (#549). All four diagnostics fire via both `log::*!` and `emit_app_log` so the signal lands in the tracing log file and the in-app activity log.
- **Companion downloads**: Configurable via `CompanionMode` enum (Disabled / AtmosToLossless / AtmosToLosslessAndLossy / SpecialistToLossy / AtmosToAllFormats / Custom) in `settings.rs`. Default: `AtmosToLossless` (Atmos → also download ALAC). The `plan_companions()` function in `download_queue.rs` returns a list of `CompanionTier` structs; each tier's codecs are tried in order. When companions exist, primary gets suffix (`[Dolby Atmos]` or `[Lossless]`); most universal companion uses clean filenames. `spawn_companion_downloads()` returns a `CompanionTaskHandle` (#663 — wraps a `JoinHandle` + `Arc<AtomicBool>` cooperative-cancel flag) tracked by the completion task; queue item stays in `Processing` state until all companions AND enrichment finish. Companion stdout is streamed line-by-line and emits `gamdl-output` progress events (speed/ETA/percentage visible in GlobalProgressBar). Suffix system via `codec_suffix()` (delegates to `codec_suffix_from_registry()` in `codec_registry.rs`), `apply_codec_suffix()`, and `needs_primary_suffix()` in `download_queue.rs`. Codec suffixes are defined in `codecs.toml` (e.g., ALAC → `[Lossless]`, Atmos → `[Dolby Atmos]`, AC3 → `[Dolby Digital]`, AAC Binaural → `[Binaural]`, AAC Downmix → `[Downmix]`, AAC Legacy → `[AAC Legacy]`, HE-AAC → `[HE-AAC]`); empty string means clean filenames (no suffix). **Custom mode**: Users select specific codecs via `custom_companion_codecs: Vec<SongCodec>` setting; each selected codec becomes its own `CompanionTier` (all user selections respected, including the primary codec — with native priority the actual codec may differ); the last codec in the list gets a clean filename, others get suffixes. Companion downloads are **skipped when the primary download fails with a network error** — since the network is unavailable, companions would also fail (wasting ~3 min per item on GAMDL timeouts). Activity log: "Companion downloads skipped — network unavailable". Per-tier codec details are logged before spawning ("Companion tier 0: trying ac3, alac, aac"), per-codec attempts during execution ("Companion tier 0: attempting ac3..."), and per-codec failures on non-zero exit ("Companion tier 0: ac3 failed — {error}"). Desktop notifications deferred until the completion task finishes (all companions + enrichment). **Companion lyrics conversion** (`run_companion_lyrics_conversion()`) uses `find_dirs_with_ttml()` to recursively walk the output directory tree and locate album directories containing `.ttml` files, then runs all four conversion services (Enhanced LRC, Rich SRT, WebVTT, ASS) on each discovered directory — this is necessary because the output path is the top-level directory (e.g., `~/Music/`) but TTML files live in `Artist/Album/` subdirectories.
- **Metadata enrichment**: After GAMDL writes standard tags, a unified background task runs 12 enrichment stages via `metadata_tag_service.rs`, `enhanced_lyrics_service.rs`, `webvtt_service.rs`, `rich_srt_service.rs`, `ass_subtitle_service.rs`, `acoustid_service.rs`, `replaygain_service.rs`, `apple_music_api.rs`, `mediainfo_service.rs`, and `musicbrainz_service.rs`: (1) codec/source/channel tags + Apple Music API metadata (always-on; uses ffprobe-based codec detection via `detect_audio_info()` + `resolve_codec_from_ffprobe()` when native priority is active to determine the ACTUAL codec GAMDL selected, rather than trusting the requested codec), (1a) API response JSON dump (verbose-only diagnostic — writes `<AlbumName>-applemusic-data.json` to the album output directory when `verbose_activity_log` is enabled, containing the raw Apple Music API response for debugging endpoint changes), (1b) syllable-lyrics API fetch (automatic when Enhanced LRC is on — upgrades TTML to word-level timing via `/syllable-lyrics` endpoint if GAMDL's output lacks it), (2) Enhanced LRC conversion (opt-in, `enhanced_lrc`, default on), (2b) lyrics format fallback (opt-in, `lyrics_fallback_enabled`, default on — retries with LRC/SRT if TTML didn't produce lyrics for all tracks), (2c) WebVTT subtitle generation (opt-in, `generate_webvtt` — converts TTML/SRT/LRC to `.vtt`), (2d) Rich SRT generation (opt-in, `generate_rich_srt`, default on — TTML → SRT with bold/italic/underline/colour styling tags, overwrites plain SRT), (2e) subtitle embedding (opt-in, `embed_subtitles` — embeds SRT/VTT sidecar content as freeform atoms in MP4 containers), (2f) ASS subtitle generation (opt-in, `generate_ass` — converts TTML/SRT/LRC to `.ass` with styled formatting), (3) animated artwork download (reuses API response), (3b) artist promo video download (opt-in, `artist_promo_video_enabled` — downloads the animated background from the artist's Apple Music page as `ArtistCover.mp4` to the artist folder; uses `artist_id` from `AlbumMetadata`; skipped if already exists), (4) AcoustID fingerprinting (opt-in, `acoustid_enabled`), (5) ReplayGain analysis (opt-in, `replaygain_enabled`, with configurable `replaygain_album_gain` toggle for album-level tags; supports MP4-family via `mp4ameta`, FLAC/OGG/Opus via `lofty` Vorbis Comments, MP3 via `lofty` ID3v2 TXXX frames, MKV/WebM/OGV video containers via `lofty` #329), (6) music video companion downloads (opt-in, `music_video_companion`, requires MusicKit credentials), (6b) MusicBrainz video discovery fallback (opt-in, `musicbrainz_lookup` — 3-tier: Apple Music URL → ISRC → AcoustID recording ID). All use `mp4ameta` crate freeform atoms via `set_data()` + `write_to_path()` which preserves existing tags. Shared API logic in `apple_music_api.rs` (MusicKit JWT, URL parsing, catalog fetch, music video relation lookup). The `AlbumMetadata` struct (20+ fields including record label, copyright, editorial notes, release dates, compilation/single/complete flags, MFiT status, track count) and `TrackMetadata` struct (20+ fields including ISRC, audioTraits, Apple Digital Master flag, composer, duration, genres, play params, URLs) are fetched once and shared across enrichment, artwork, and music video companion lookup to avoid duplicate API calls. All Apple-specific metadata is embedded under the `MeedyaMeta` freeform atom namespace with `Apple` prefix (e.g., `MeedyaMeta:AppleRecordLabel`, `MeedyaMeta:AppleDigitalMaster`). **AcoustID API key**: Resolved via `resolve_api_key()` in `acoustid_service.rs` — user override (Settings > Metadata) takes priority, then compile-time embedded key (`option_env!("ACOUSTID_API_KEY")`), then `None` (skipped). Release builds embed the key from a GitHub Actions secret. The `has_embedded_acoustid_key` Tauri command lets the frontend detect whether a built-in key exists.
- **WebVTT subtitles**: When `generate_webvtt` is enabled (default: `false`), enrichment Step 2c generates WebVTT (`.vtt`) subtitle files from existing lyrics sidecars. Source priority: TTML (richest timing, with `<span>` word-level support), SRT (has start+end times), LRC (start times only, 3s estimated durations). Service in `webvtt_service.rs`, toggle in Settings > Lyrics.
- **Lyricsfile (`.lyrics`) YAML sidecars** (#596): When `generate_lyricsfile` is enabled (default: `false` — the format is officially experimental per LRCGET 2.0 release notes), enrichment **Step 2g** (between Step 2f ASS and Step 3 animated artwork) converts the TTML sidecar emitted by GAMDL into a Lyricsfile YAML sidecar via `meedya_lyrics::Lyricsfile::from_ttml(...).to_yaml()` (shared upstream crate, MeedyaSuite-core PR #36). Preserves Apple Music's word-level timing in a vendor-neutral, plain-text-editable format that LRCGET and LRCLIB consume directly. Service in `lyricsfile_service.rs`, toggle in Settings > Lyrics, idempotent (won't overwrite an existing `.lyrics` file — preserves user edits made in LRCGET). Manifest schema gains `ManifestTrack.has_lyricsfile: bool` (album-level granularity populated via a disk scan inside `write_manifest`; per-track precision is a follow-up that would require running the GAMDL filename-template engine to map title→stem). Help doc: `help/lyrics-and-metadata.md` "Lyricsfile (.lyrics) — Open YAML Format" section.
- **MusicBrainz lookup**: When `musicbrainz_lookup` is enabled (default: `false`), enrichment Step 6b discovers music videos and cross-platform URLs via the MusicBrainz database. Uses a 3-tier priority chain: (1) Apple Music URL search in MB external links (highest fidelity), (2) ISRC code search (standard identifier), (3) AcoustID recording ID direct lookup (from Step 4 fingerprinting). No credentials required (free public API). Rate-limited to 1 request/second. Returns `MusicBrainzRecording` with `external_urls: HashMap<String, String>` for cross-platform discovery (Apple Music, YouTube, Spotify, Deezer, Tidal, SoundCloud, Bandcamp). Storefront-aware: rewrites Apple Music URLs to match the user's configured storefront. Service in `musicbrainz_service.rs`, toggle in Settings > Quality > Video Quality.
- **Music video companions**: When `music_video_companion` is enabled (default: `false`) and MusicKit credentials are configured, enrichment Step 6 queries the Apple Music API for music video relationships (`fetch_music_video_relations()` in `apple_music_api.rs`). Songs with music videos get a companion GAMDL download spawned with the music video URL, using the user's video quality settings. Deduplicated by video ID. Fire-and-forget (failures logged, don't affect primary download). Toggle in Settings > Quality > Video Quality, greyed out when MusicKit credentials not configured.
- **Enhanced LRC**: When `enhanced_lrc` is enabled (default: true), `merge_options()` Layer 4 forces TTML as the primary lyrics format so GAMDL preserves Apple Music's word-level timing data. After download, `enhanced_lyrics_service.rs` parses the TTML via `roxmltree`, extracts `<span>` word timestamps from `itunes:timing="Word"` documents, and generates Enhanced LRC with inline `<mm:ss.xx>` word timing. The `.lrc` sidecar is saved and embedded in M4A/M4V via the `©lyr` atom. Songs without word-level timing gracefully fall back to standard line-level LRC. **Syllable-lyrics API fallback** (enrichment Step 1b): When GAMDL's TTML output lacks word-level timing (`itunes:timing="Word"`), the enrichment pipeline fetches syllable-level TTML directly from Apple Music's `/syllable-lyrics` API endpoint (`fetch_syllable_lyrics()` in `apple_music_api.rs`). Requires both MusicKit developer credentials (JWT) and a `Music-User-Token` extracted from imported browser cookies (`extract_media_user_token()` parses the Netscape cookies file with expiry validation). Upgraded TTML sidecars are written before Enhanced LRC conversion runs. Rate-limited at 200ms between requests. Silently skipped when credentials or cookies are unavailable.
- **Lyrics embed + sidecar**: When enabled in settings, `merge_options()` forces `no_synced_lyrics=false` and removes `"lyrics"` from `exclude_tags` to ensure both embedded lyrics and sidecar files are created.
- **Content advisory suffixes**: When `content_advisory_in_filenames` is enabled (default: `true`), `apply_advisory_suffixes()` in `metadata_tag_service.rs` renames album folders and track files with `[Explicit]` or `[Clean]` suffixes after metadata is fetched during enrichment. Per-track granularity via `TrackMetadata.content_rating`; album folder via `AlbumMetadata.content_rating`. Advisory suffix inserted BEFORE codec suffix: `01 Title [Explicit] [Lossless].m4a`. Idempotent (won't double-suffix). Queue item's `output_path` updated after folder rename so "Open Folder" still works. Toggle in Settings > Metadata.
- **Queue progress indicators**: Queue header statistics bar displays active/queued/completed/failed counts with an aggregate progress bar showing the completed/total ratio. `QueueItem` shows a "Track N of M" counter for album downloads, extracted from GAMDL progress output.
- **Queue auto-start**: Controlled by `auto_start_queue` setting (default: `true`). When enabled, `process_queue()` is called immediately after enqueue in `start_download()`, `retry_download()`, and `import_queue()`. When disabled, items stay in `Queued` state until the user clicks "Start Queue" in the Queue page (calls `process_queue_manual` command). Startup queue recovery always runs regardless of this setting.
- **Temp directory**: `temp_path` setting (default: empty string, resolved to `{OS temp dir}/MeedyaDL`). Resolved at runtime in both `merge_options()` and `settings_to_ini()` via `std::env::temp_dir().join("MeedyaDL")`. Avoids GAMDL's default of `.` which is unwritable on macOS from `/Applications`. Configurable in Settings > Tools.
- **Queue persistence**: The download queue is saved to `{app_data_dir}/queue.json` after every mutation (enqueue, cancel, retry, clear, completion, error, fallback). On startup, `load_queue_from_disk()` restores items and `process_queue()` is called after a 2-second delay (to let frontend event listeners initialise). Active items (Queued/Downloading/Processing) and failed items (Error) are persisted; only Completed and Cancelled items are cleared on restart. Failed items are restored in their Error state with the original error message so the user can review the failure and manually retry — they are not auto-retried. `PersistedQueueItem` includes an optional `error` field (`#[serde(default)]` for backwards compatibility with older `queue.json` files). Uses clone-then-release pattern: clone persistable items from lock, release lock, then write to disk. **Important**: Queue restoration in `setup_queue_recovery()` uses `blocking_lock()` (not `block_on(lock().await)`) and `tauri::async_runtime::spawn` (not `tokio::spawn`) because the Tokio runtime may not be registered as "current" during the `setup` closure on macOS.
- **Queue export/import**: Export via `export_queue` command opens native save dialog with `.meedyadl` filter; writes `QueueExportFile` JSON (version, app, exported_at, items). Import via `import_queue` opens native file picker, validates schema version == 1, re-enqueues items with fresh settings merge. Exported items contain only URLs + per-download overrides (not merged options), so the importing device uses its own settings as base. **Clear All**: `clear_all_queue` IPC command with confirmation modal removes all non-active items (queued, completed, cancelled, errored); active downloads preserved. Separate from "Clear Completed" which only clears complete+cancelled.
- **Hidden animated artwork**: After downloading FrontCover.mp4/FrontCoverPortrait.mp4/ArtistSpotlightCover.mp4, files are hidden via OS-native mechanisms if `hide_animated_artwork` is `true` (default). macOS: `chflags hidden` (preserves filename); Windows: `attrib +H` (preserves filename); Linux: `.` prefix rename (changes filename). Logic in `animated_artwork_service::hide_file()`, called from the artwork background task in `download_queue.rs`. Portrait variant was renamed from `PortraitCover.mp4` → `FrontCoverPortrait.mp4` post-v0.38 so it sorts adjacent to `FrontCover.mp4`; legacy files on disk are NOT auto-renamed.
- **Artist promo video**: When `artist_promo_video_enabled` is enabled (default: `false`), enrichment Step 3b downloads the animated background video from the artist's Apple Music page. Uses `fetch_artist_promo_video()` in `apple_music_api.rs` which queries `/v1/catalog/{storefront}/artists/{artist_id}?extend=editorialVideo` for HLS video URLs. Priority order: `motionArtistFullscreen16x9` → `motionArtistWide16x9` (two 16:9 artist-framed feeds only; lower tiers like `motionDetailSquare` / `motionDetailTall` are deliberately excluded because they're tightly cropped around album cover art and look wrong as an artist-page hero — we prefer to skip over a visually mismatched fallback). Saved as `ArtistSpotlightCover.mp4` (renamed from `ArtistCover.mp4` in #455) in the artist folder — resolved from album directory via parent traversal with validation (#453). Idempotent — skipped if file already exists. Toggle in Settings > Cover Art (only visible when animated artwork is enabled).
- **Music video filename resolution** (#527 #531 #537): Four-tier cascade for MV placement on disk. Tier 1 (GAMDL's iTunes Lookup with album linkage → native `album_folder_template`, MV lands beside audio tracks), Tier 2 (Apple Music Catalog `music-videos/{id}?include=albums` → planned, not yet wired), Tier 3 (MeedyaDL-known parent album context → planned), Tier 4 (safety net — `MV_NO_ALBUM_FOLDER_TEMPLATE = "{artist}/Music Videos"` + `MV_NO_ALBUM_FILE_TEMPLATE = "{title} ({title_id})"` in `download_queue.rs`). `{title_id}` is Apple Music's numeric MV ID — guarantees uniqueness for same-title MVs (Clean/Explicit cuts, remixes, live versions) AND deterministic dedup on re-downloads (a datetime suffix would cause every re-download to create a new file). Only Tier 4 is implemented in the current PR; Tiers 2 & 3 tracked as follow-ups in #537. Full spec in `DEV_NOTES.md` → "Music-Video Filename & Folder Resolution".
- **Dependency manager**: 4 required external tools (FFmpeg, mp4decrypt, N_m3u8DL-RE, MP4Box). `tool-versions.toml` defines minimum supported versions per tool and mirror config (compiled into binary via `include_str!()`). Download resolution follows a three-tier fallback chain: **System PATH** → **Primary upstream** → **Mirror repo** (`MeedyaSuite/MeedyaDL-Tools`) → Error with guidance. `resolve_github_release_asset()` is the generic GitHub API resolver used by both upstream (N_m3u8DL-RE) and mirror queries. `install_tool()` first checks system PATH for a compatible version; if found, copies it to the managed tools dir and writes a `.source` marker file ("system"); otherwise downloads (with mirror fallback) and writes "managed". `DependencyStatus.source` field (Rust + TypeScript) drives the "System" badge in the setup wizard. MP4Box uses platform-specific installers (macOS Homebrew/`.pkg`, Windows NSIS `.exe`, Linux x86_64 `.deb`, Linux ARM `apt-get install gpac`) with mirror fallback via `install_mp4box_with_fallback()`. Mirror assets use standardized naming: `{tool_id}-{os}-{arch}.{ext}`. AcoustID fingerprinting flows through the shared `meedya-fingerprint` crate from `MWBMPartners/MeedyaSuite-core` (#353 Phase 3) — the `chromaprint` cargo feature pulls in `rusty-chromaprint` + `symphonia` upstream so MeedyaDL no longer carries those direct deps. Still pure Rust, no external fpcalc binary needed; the ARM Linux property (no fpcalc binaries available there) is preserved.
- **Periodic update checks**: `update_check_interval_hours` setting (default: `6`, configurable in Settings > General when "Auto-Check for Updates" is enabled). Options: Startup only (0), Every hour (1), Every 6 hours (6), Every 12 hours (12), Every 24 hours (24). Implemented as a `setInterval` timer in App.tsx Effect 4 alongside the existing startup check. The interval reads settings imperatively via `getState()` and guards against concurrent checks with `isChecking`. Changes take effect on app restart. Value `0` disables the periodic timer (startup check only). Rust field: `update_check_interval_hours: u32` with `#[serde(default = "default_update_interval")]` for backwards compatibility.
- **Updates page**: Dedicated sidebar page (`src/components/updates/UpdatesPage.tsx`) showing full release notes rendered via `react-markdown`. Strips the "Choose your download" section from release bodies since the in-app updater handles downloads. Connected to the update banner's "View Details" link and the sidebar footer's update button. External links in rendered release notes use a custom `ReactMarkdown` `components.a` handler that intercepts HTTP(S) clicks and opens them in the system default browser via `@tauri-apps/plugin-shell` (same pattern as `HelpViewer.tsx`). The "View on GitHub" buttons in both `UpdatesPage.tsx` and `UpdateBanner.tsx` also use the shell plugin instead of `window.open()`.
- **Drag-and-drop URL input**: Content area in `MainLayout.tsx` accepts Apple Music URLs dragged from browsers. Uses `dragenter`/`dragleave` counter pattern to prevent overlay flicker. Validates via `parseAppleMusicUrl()`, navigates to Download page and populates input on valid drop.
- **Batch URL paste**: `DownloadForm.tsx` uses `<textarea>` for multi-URL input. Each line validated as Apple Music URL, queued as separate download. Enter submits, Shift+Enter inserts newline.
- **Download history**: `history_service.rs` persists download history as JSON (max 1000 entries). `HistoryPage.tsx` displays with search, status icons, codec badges. Sidebar nav between Queue and Activity.
- **Duplicate URL detection**: `normalize_url_for_dedup()` and `has_duplicate_urls()` in `download_queue.rs`. Warning toast on duplicate, download still allowed.
- **Pre-queue track-level duplicate detection** (#510): `services/duplicate_detector.rs`. When an Apple Music artist URL is expanded via `artist_auto_select_multi` into N queue items (album + singles-eps + compilation + …), the Apple Music catalog API is queried to enumerate each mode's tracks, and duplicates are removed per user preference so any given song is only downloaded once. `fetch_artist_albums()` + `classify_album_bucket()` in `apple_music_api.rs` enumerate + bucket albums (main / singles-eps / compilation / live). Preference order (default: main-albums > singles-eps > compilation-albums > live-albums > top-songs), scope (off / intra-session / intra+queued / intra+queued+history) and key strategy (song_id+ISRC fallback / ISRC only / song_id only) are configurable in Settings > Quality > Duplicate Detection. Does NOT affect companion downloads (ALAC/Atmos/AAC variants of a song still run); only skips same-quality duplicates across modes. Winning mode's queue item is rewritten from the artist URL to per-track URLs (`album_url?i=song_id`); modes with zero unique tracks are suppressed entirely. API / credential failures fall back to the original artist URL (never blocks a download). `ManifestTrack.song_id` added so the History scope can match previously-downloaded songs. `DuplicateDetectionSettings` default → on (`intra_and_queued`) via settings schema migration v1 → v2.
- **Native desktop notifications**: `tauri-plugin-notification`. Triggers on download complete/error and queue complete. `desktop_notifications` setting (default: `true`). **Notification style**: `notification_style` setting (default: `native_and_in_app`) controls delivery method — `in_app_only` (toast banners only), `native_and_in_app` (both OS notifications and toasts), `native_only` (OS notifications only). Configurable in Settings > General > Notifications.
- **Deep links**: `meedyadl://` custom URL scheme via `tauri-plugin-deep-link`. Parses Apple Music URLs and navigates to Download page.
- **Clipboard monitoring**: `useClipboardMonitor.ts` hook polls system clipboard every 2 seconds via `clipboard_service.rs` (uses `arboard` crate). When a supported URL (Apple Music) is detected, clicking **Download** adds it directly to the queue via `startDownload()` (no longer pre-fills the URL input). When the window is NOT focused, a native OS notification is sent via `@tauri-apps/plugin-notification` so the user sees the detection even when MeedyaDL is minimised. Session-scoped deduplication (same URL won't re-prompt). Privacy-first: only checks for URL patterns, never stores clipboard contents. `clipboard_monitoring` setting (default: `true`), toggle in Settings > General > Preferences. Native notifications gated by `desktop_notifications` setting. IPC command: `read_clipboard` in `commands/clipboard.rs`.
- **Keyboard shortcuts**: `useKeyboardShortcuts.ts` hook. Cmd/Ctrl+D (download), Cmd+, (settings), Cmd+Q (queue).
- **Settings sidebar categories**: Settings page sidebar grouped into sections (General, Download, Authentication, System) with headers.
- **Accessibility**: High-contrast mode (`high_contrast` setting, `a11y-high-contrast.css`), colour blindness themes (`colour_blind_mode`: normal/deuteranopia/protanopia/tritanopia, `a11y-colour-blind.css`), `prefers-reduced-motion` media query, WCAG 2.4.1 skip navigation link. `.hintrc` suppresses Edge DevTools false positives.
- **Storefront configuration**: `storefront` setting in AppSettings. Auto-derived from `language` region code (e.g., en-GB → gb), fallback "us". Configurable in Settings > General. The setting drives MeedyaDL-side URL injection (`normalize_apple_music_url` for non-geographic URLs) and storefront-fallback retries (#666); GAMDL itself derives the storefront from the URL path via `VALID_URL_PATTERN`. **No longer written to GAMDL's config.ini on supported releases (#881)** — git archaeology confirmed `storefront` has never been a CLI/INI option in any GAMDL version ≥ 2.9.1, and `cleanup_unknown_params()` strips it on read. The dead INI write is suppressed behind `GamdlFeature::StorefrontIniKeyStripped` (true for every supported version, false for unknown/pre-2.9.1 to preserve pre-#881 behaviour).
- **Global progress bars**: `GlobalProgressBar.tsx` renders dual progress bars between `<main>` and `<StatusBar>` in `MainLayout.tsx`. Upper = per-item progress (accent blue), lower = queue-level (green). Auto-hides when no downloads active. Shows percentage for both bars. Apple Music platform icon (inline SVG via `detectPlatform()` with config from `engines.toml` via IPC) displayed next to track name, extensible for future services. **Caption format**: "DOWNLOADING... Artist — Album — Track" with state prefix and full context from early Apple Music API metadata fetch at download start (before GAMDL subprocess). `album_name` and `artist_name` fields on `QueueItemStatus` populated via `try_fetch_metadata()` (public in `metadata_tag_service.rs`) at enqueue time. **Companion speed/ETA**: During companion downloads, the per-item bar shows speed, ETA, and percentage (not indeterminate) — the store preserves `processing` state stability so queue-level progress doesn't oscillate. Speed/ETA are cleared on `processing_step` transitions to avoid stale data during enrichment. `processing_label` field on `QueueItemStatus` drives the label display (e.g., "Enriching metadata...", "Companion: ALAC 14/28"). Queue-level bar counts `processing` items as "done" for partial credit.
- **Activity log search & filtering**: Search input + category filter toggles (System, Download, Verbose) in `ActivityLog.tsx`. Uses `useMemo` for filtered entries. Virtualizer (`useVirtualizer` from `@tanstack/react-virtual`) renders only visible rows + 50-row overscan buffer. Auto-scroll uses `virtualizer.scrollToIndex()` for accurate positioning with dynamic row heights.
- **ISRC handling**: `extract_isrc_from_vendor()` in `metadata_tag_service.rs` with 3-case logic: (1) ISRC blank → copy from Vendor, (2) ISRC set + differs → store both, (3) match or no Vendor → no-op.
- **Codec suffix post-download rename**: `apply_codec_rename_suffix()` in `metadata_tag_service.rs`. Template-based approach broke GAMDL; files are renamed after download instead. `enrich_single_file()` returns the ffprobe-detected `SongCodec`; the post-enrichment suffix rename uses the per-file detected codec (not the requested codec). This prevents incorrect suffixes when native priority falls back silently (e.g., `[Dolby Atmos]` on ALAC files).
- **i18n groundwork**: Uses `i18next` + `react-i18next` + `i18next-browser-languagedetector`. Translation files in `public/locales/{lang}/translation.json` (currently en, de, fr). Language setting `ui_language` in AppSettings (empty = auto-detect from OS). Language dropdown in Settings > General (Appearance section). `initI18n()` called during app startup in App.tsx. To translate a component: `const { t } = useTranslation()` → `t('key')`. To add a language: create locale JSON, add to `AVAILABLE_LOCALES` in `i18n.ts` and `UI_LANGUAGE_OPTIONS` in `GeneralTab.tsx`.
- **Brand assets**: All brand files in `assets/brand/` (logo.svg, logotype.svg, icon.svg, brandkit.html + PNG/ICO/ICNS variants). Brand assets are **proprietary** (not MIT) — do not change license headers. Sidebar uses `<img>` tags for animated SVG logo + logotype (CSP-safe, blocks SVG script execution). Colour-blind mode passed via `?mode=cb-{colourBlindMode}` from settings store. Dark mode auto-detected via `@media(prefers-color-scheme)` in SVG CSS. Regenerate with `node scripts/generate-icons.mjs` (icons) and `node scripts/svg-to-apng.mjs` (animated PNGs). Copyright year: `2026` (dynamic via `scripts/update-copyright-year.sh`).
- **Third-party licence obligations when bundling**: The offline-installer build path (`release.yml` Step 8.5, `bundle_engines=true`) redistributes upstream engines and tools inside our signed installer. MIT/BSD/Unlicense/PSF components only need the upstream copyright + permission notice preserved alongside the binary. **LGPL components** (FFmpeg, MP4Box/GPAC) additionally need a written offer for source (component + exact version + upstream-source URL + fall-back contact, valid for three years) and the binary must remain user-substitutable — subprocess invocation already satisfies this. **GPL components** (get_iplayer, planned for M8) need the complete corresponding source shipped alongside the binary (or a three-year written offer); MeedyaDL's own MIT code is protected from GPL propagation by the "mere aggregation" exception because we subprocess-invoke rather than link. The harvesting + source-offer emission belongs in `release.yml` Step 8.5 itself so it can never be forgotten when a new component lands in `engines.toml` or `tool-versions.toml`. Full matrix + practical "written offer for source" template in `.claude/memory/project_third_party_licence_obligations.md`. Tracked in #802. Notes do **not** apply to the default tiny-installer path (`bundle_engines=false`) which only ships MeedyaDL's own code — engines copy themselves from PyPI / GitHub Releases onto the user's machine at first launch.
- **Licence compliance is enforced per-PR (#806)**: the `Licences` workflow (`.github/workflows/licences.yml`) runs on every PR to `main` and on `workflow_dispatch`. Three checks: (1) `npm run check:acknowledgements` confirms every direct dep in `Cargo.toml` / `package.json` is named in `ACKNOWLEDGEMENTS.md` (set-coverage drift); (2) `npm run check:upstream-licences` confirms each direct dep's actual upstream-declared licence string matches what `ACKNOWLEDGEMENTS.md` claims, catching upstream re-licensings between MeedyaDL releases (most commonly MIT → MIT/Apache-2.0 dual-licence flips, but also the rare permissive → copyleft change that would be a serious compliance risk); (3) `cargo-deny check licenses` enforces the permissive-only allowlist in `src-tauri/deny.toml` on the full transitive tree. The upstream-string check uses an SPDX-aware normaliser that treats `LGPL-3.0+` ≡ `LGPL-3.0-or-later` and `MIT/Apache-2.0` ≡ `MIT OR Apache-2.0` as equivalent (advisory, not blocking). The `Licences` workflow is in addition to (not a replacement for) the existing cargo-deny step inside `ci.yml::backend` — duplicated here so the compliance picture lives in one workflow a maintainer can read in isolation. Run locally via `npm run check:legal` (umbrella over both Node scripts).
- **PR security heuristics run per-PR**: the `PR Security Checks` workflow (`.github/workflows/pr-security.yml`) runs on every PR to `main` / `release-candidate` / `beta` / `alpha` and on `workflow_dispatch`. **All checks are non-blocking advisory** — the merge gate stays with `ci.yml` (which already hard-gates `cargo clippy -D warnings`, `cargo test`, `cargo-deny`, `tsc`, `eslint`, and CodeQL for JS/TS + Actions); this workflow adds the *heuristic* layer those gates don't cover and posts a **single upserted PR comment** (edited in place by hidden marker `<!-- meedyadl-pr-security -->`, not a new comment per push). Adapted from the WebMS-Intra `pr-security.yml` to MeedyaDL's Rust/TS/Tauri stack. Eight checks: (1) gitleaks CLI secrets scan (working tree + commits since base, `--redact`, findings surfaced in the comment, no SARIF upload so the permission surface stays `contents:read` + `pull-requests:write`); (2) Rust subprocess shell-interpolation grep (`Command::new("sh")` / `.arg("-c")` — enforces the "no `sh -c`" invariant); (3) `unsafe` Rust blocks/fns in non-test changed code; (4) dangerous frontend sinks (`eval` / `new Function` / `dangerouslySetInnerHTML` / `innerHTML=`); (5) hardcoded absolute paths (`/Users/`, `/home/<user>/`, `C:\`); (6) unpinned GitHub Actions (`uses: org/repo@<tag>` not a 40-hex SHA — handles both `- uses:` and bare `uses:` forms, exempts `./local` and `docker://`); (7) sensitive/proprietary path touches (`assets/brand/` is PROPRIETARY, `src-tauri/capabilities/`, `tauri.conf.json`, `.github/workflows/`, signing/entitlements); (8) cross-source consistency via `tools/audit-checks/`. Checks 2–7 scan only PR-changed files; check 8 validates whole-repo state. The two consistency scripts — `check_ipc_commands.py` (every `#[tauri::command]` is registered in `lib.rs` `generate_handler![]` AND every frontend `invoke('x')` targets a registered command — the runtime "command not found" class) and `check_codec_registry.py` (every meta-codec `resolves_to` target is a real codec section AND every audio `services.gamdl` flag is a kebab-case `SongCodec` variant) — are pure cross-source reference validators (the MeedyaDL analog of WebMS's `check_route_targets.py` / `check_sql_columns.py`), exit 0 by default and 1 under `--strict`, report `  • path:line — msg` bullets the workflow greps for, and **must stay zero-finding on a clean tree** (add a negative test when changing them). Run locally: `python3 tools/audit-checks/check_ipc_commands.py` / `check_codec_registry.py`. The matching **`.github/pull_request_template.md`** carries the manual security-review checklist (subprocess safety, IPC contract registration, keychain/redaction, CSP/capabilities, licensing, proprietary-asset headers) that a grep can't cover.
- **Git operations**: Do NOT auto-commit or auto-push. Only edit files — let the user control git operations.
- **Documentation maintenance**: When adding features, modifying settings, changing commands/services, or altering UI — update ALL affected markdown files (README.md, Project_Plan.md, CHANGELOG.md, CLAUDE.md, help/*.md). This includes version numbers, file counts, feature lists, project structure trees, and help topic cross-references. Project_Plan.md serves as both the plan and status tracker (PROJECT_STATUS.md was consolidated into it).
- **GitHub Issue tracking**: For every task (features, bug fixes, enhancements, security fixes): (1) Create a GitHub Issue if one doesn't exist (`gh issue create`); (2) Close with completion comment when done (`gh issue close --reason completed`); (3) Link parent/child issues in the body (e.g., "Depends on #107", "Part of #100"); (4) Add to "MeedyaDL Development" project (`gh project item-add 6 --owner MWBMPartners`); (5) Create follow-up issues for any future work identified during implementation.
- **Shared Claude memory**: Project-scoped Claude Code memory (`type: project` files — RC milestone state, multi-service groundwork status, GAMDL audit cadence, etc.) is committed to `.claude/memory/` so every contributor's Claude session loads the same project context. Personal memory (`type: user`, `type: feedback`) stays in `~/.claude/projects/<sanitised-path>/memory/` and is never committed. Bootstrap on a new dev machine: `./scripts/sync-claude-memory.sh`. Re-run after `git pull` whenever a teammate updates a shared memory file. The script copies repo→home only (one-way); to share a change, edit the file under `.claude/memory/` and commit. Index hooks merge under sentinel markers in the personal `MEMORY.md` so re-runs are idempotent and personal entries are preserved. See `.claude/memory/README.md` for the full convention.
- **Internal helpers (audits v1 + v2, all landed by 2026-05-10)**: New code should reach for these primitives instead of hand-rolling the same shape. Backend: `walk_dir_depth` / `walk_dir_find_first` (`utils/fs_walk.rs`) for depth-bounded recursive walks; `build_client` / `build_simple` (`utils/http_client.rs`) for `reqwest::Client` construction; `atomic_write_json` (`utils/atomic_write.rs`) for durable JSON writes; `spawn_line_reader` (`utils/subprocess_reader.rs`) for `BufReader → next_line → visitor` subprocess shells; `context_err!(result, "msg {fmt}")?` (`utils/error_context.rs`, `#[macro_export]` so the call site is `crate::context_err!`) for `.map_err(|e| format!("...: {e}"))?`. Frontend: `createAsyncResourceStore<T>(config)` (`src/lib/createAsyncResourceStore.ts`) for IPC-backed Zustand stores; `withErrorToast(fn, opts)` (`src/lib/withErrorToast.ts`) for try/catch + toast; `useConfirmation({ title, description, onConfirm })` (`src/lib/useConfirmation.tsx`) for confirmation modals; `useSettingsField<K>(key)` (`src/hooks/useSettingsField.ts`) for per-control settings binding (replaces `settings.X` + `(v) => updateSettings({ X: v })` lambda pair); `useAsyncTask(fn)` (`src/hooks/useAsyncTask.ts`) for component-local async lifecycle; `makeFixture` / `makeQueueItem` / `makeActivityEntry` / `makeScannedManifest` (`src/testing/fixtures.ts`) for test fixtures. Full catalogue + rationale in `.claude/memory/project_audit_v2_helpers.md`.
- **Release pipeline gotchas**: Three recurring failure modes from the 2026-05-09 v1.1.0 cut. **(a) `[skip ci]` propagates through release-please's CHANGELOG body** into the merge commit message → GitHub Actions skips every push-triggered workflow → no Release Please run, no tag, no Release build. Recovery: `gh workflow run "Release Please" --ref main` then `gh workflow run "Release" -f tag=vX.Y.Z`. **(b) "Release in progress…" placeholder persistence** — `release.yml`'s per-platform race-guard creates the GitHub Release with `--notes "Release in progress..."`, then the "Append download guide" step appends to it instead of replacing. Affects every prerelease that didn't go through release-please-action. PR #741 patches the workflow + 13 historical releases were backfilled. **(c) Manually-tagged stable releases sit as drafts** — `release.yml`'s auto-publish step only handles prereleases; stable tags rely on `release-please-action`, which isn't involved when you tag manually (e.g., promoting an RC). After the build, run `gh release edit vX.Y.Z --draft=false`. Full recovery patterns + the don't-auto-flip-stable-flags policy in `.claude/memory/project_release_pipeline_gotchas.md`.
- **Wrapper triangle (`wrapper_account_url` + `wrapper_m3u8_ip` + `wrapper_decrypt_ip`)**: GAMDL's wrapper integration uses three independent connections — account API (HTTP, default `http://127.0.0.1:30020`), m3u8 socket (TCP, `127.0.0.1:20020`, GAMDL v3.1+ only), decryption socket (TCP, `127.0.0.1:10020`, all GAMDL releases). All three live in `AppSettings` and are surfaced in Settings > Advanced > Wrapper. The third (`wrapper_decrypt_ip`) was added in #743 / #744 — before that, remote-wrapper LAN setups silently failed at the decryption stage because GAMDL fell back to its compile-time `127.0.0.1:10020` default. Each setting drives a 3-second TCP-connect preflight check (`check_wrapper_health`, `check_wrapper_m3u8_health`, `check_wrapper_decrypt_health` in `health_check_service.rs`) that surfaces yellow toasts at queue time. Settings schema bumped 4 → 5 in #744 to ratchet the new field. When users report "wrapper auth doesn't work on my Pi", check that all three addresses point at the Pi, not just one or two. **Note (2026-07-10):** on wrapper-v2 0.0.2 (GAMDL 3.8.2+), decrypt is a separate raw-TCP port again (not HTTP) — `wrapper_decrypt_ip` now drives `--wrapper-decrypt-host`/`--wrapper-decrypt-port`.

## Release Workflow

```text
Push fix:/feat: commits directly to main
  → release-please creates/updates a Release PR (bumps versions)
  → User reviews and merges the Release PR
  → release-please creates tag (e.g., v0.3.0) using RELEASE_PAT
  → release.yml triggers → 6 platform builds → draft GitHub Release
  → changelog.yml triggers → git-cliff regenerates CHANGELOG.md
```

Manual override: `version-bump.yml` + `scripts/bump-version.mjs` for non-standard releases.

### MANDATORY: user-facing ("ELI5") release notes (#1028)

Git-cliff/release-please render notes from technical commit subjects — useless to end users. The GitHub Release body is what the in-app updater serves via `release_body` in `update_checker.rs::UpdateCheckResult`, so it MUST be plain-English (`.github/release-notes/STYLE_GUIDE.md`: no code identifiers / flags / file names / issue-jargon; "what will I notice, or what can I now do?"). The old manual "rewrite the release-please PR body" step was missable — and was missed (v1.5.0, v1.10.1) — so it's replaced by a durable mechanism (**CHANGELOG.md stays technical** — the developer record; GitHub Releases are the user-facing record):

1. **Every `feat`/`fix`/`perf` PR body ends with `Release-Note:` trailer line(s)** — one plain-English sentence per user-visible change (`Release-Note: none` when nothing is user-visible). This repo squash-merges with PR_BODY, so the trailer lands in the commit footer, and git-cliff renders it as the release notes (What's new / What's fixed / Performance / Notes) via `.github/cliff-eli5-body.tera`, with the technical changelog collapsed into `<details>`. Put the trailer(s) in the LAST block of the body — putting them *after* the `🤖 Generated with Claude Code` line is fine (the template takes only the trailer's first line, `split(pat="\n") | first`). The `Release Note Gate` CI check fails a user-facing PR with no trailer. Auto-cut alpha/beta/rc need **no** human step — tier-2 renders their trailers automatically.
2. **Stable releases MUST have a curated `.github/release-notes/vX.Y.Z.md`** (gold-standard four-section ELI5 — ref `v1.11.0-alpha.18.md` / PR #785). When a `chore(main): release X.Y.Z` PR opens: run `scripts/release-notes/draft-notes.sh vX.Y.Z`, polish the `.draft` to the style guide, drop the `.draft` suffix, commit it to main. The `Release Note Gate / release-pr-notes-file` check BLOCKS the release PR until that file exists on main; `ensure-release` then applies it OVER release-please's commit-speak body (footer preserved) via `scripts/release-notes/apply-notes.sh`. Only THEN signal the release PR ready to merge.
3. **Post-hoc fix**: `bash scripts/release-notes/apply-notes.sh vX.Y.Z` re-applies the curated file to a published release (footer-preserving, idempotent). Committed `.github/release-notes/` files also self-heal: a re-run of `ensure-release` re-applies them.

Precedent: v1.5.0 (2026-05-17) and v1.10.1 both shipped raw commit-speak because the old manual step was missed. The trailer backbone + the two CI gates (PR trailer + stable notes-file) turn that omission into a red X at the two moments a human is already looking (PR review, release-PR merge), so it can't silently rot. Full design + backfill in #1028.

### Prerelease release-note generation (#1027)

`release.yml`'s `ensure-release` job builds each GitHub Release body via a 3-tier notes source: **tier 1** = a pre-staged `.github/release-notes/<TAG>.md` if present; **tier 2** = git-cliff; **tier 3** = a static "see CHANGELOG.md" fallback. For **prerelease** tags (alpha/beta/rc) tier 2 assembles TWO sections (`release.yml` ~L267-330): a detailed **"New in this build"** (full git-cliff render incl. commit bodies, `PREV_TAG..TAG`) + a compact subjects-only **"All changes since <last stable>"** (`.github/cliff-cumulative-body.tera` template + `--ignore-tags '(alpha|beta|rc|nightly|weekly|monthly)\.[0-9]'` to collapse intermediate prerelease tags into one section), so every prerelease is self-describing even when the incremental delta is dependency/housekeeping-only (a dep-only build → an honest "_No user-facing changes since … — dependency and internal housekeeping updates only._" preamble + the cumulative view). Content is detected by a `### ` heading (git-cliff renders a `## [version]` header even when every commit in the range is skipped, so `-s` is insufficient); a 100 000-byte guard truncates + links to CHANGELOG. **Stable** tags keep the original single-range render (unchanged). `cliff.toml` skips the machine pure-bump commits `^chore\((alpha|beta|rc|nightly|weekly|monthly)\): X.Y.Z-<channel>.N` (NOT blanket `chore(<channel>)` — human housekeeping like `chore(alpha): combined housekeeping…` still renders; same over-match lesson as `alpha-release.yml:73-82`), plus `chore(version)` + internal `docs(handoff)`. Before #1027, dep-only alphas rendered as just `🧹 Maintenance — (alpha) X.Y.Z-alpha.N` noise (the version-bump commit wasn't skipped); `v1.11.0-alpha.19…28` were backfilled via `gh release edit` (footer preserved). **Merge-strategy caveat:** the alpha channel is fed by `alpha-release.yml` merging `origin/feat/*` + PRs; prior PRs to `alpha` were **squash-merged**, which collapses ~N commits into one subject → the incremental section becomes a single bullet. **Merge-strategy rule (#1046):** rebase-merge a channel PR only when *every* commit in it already carries its own `Release-Note:` footer (trailers live in the PR body, and rebase-merge drops the PR body — squashing away that footer if the commits don't have their own); otherwise squash-merge (`PR_BODY`) so the PR-body trailer lands in the resulting commit's footer instead of being lost. Local dry-run: `git-cliff --strip all --tag <TAG> <RANGE>` (git-cliff pinned 2.13.1; `brew install git-cliff`). **Stale-doc correction:** the "Release Channels" section below still describes **nightly/weekly/monthly** as live cron channels — those workflows were **removed by #879**; only `alpha-release.yml` / `beta-release.yml` / `release-candidate-release.yml` remain (the `UpdateChannel` enum retains the variants; the cliff.toml skip regex keeps all six scopes so historic nightly bumps stay out of the regenerated CHANGELOG).

### Release Channels (current state)

`UpdateChannel` enum in `models/settings.rs` declares **seven** ordered variants — `Nightly < Weekly < Monthly < Alpha < Beta < Rc < Stable` (`PartialOrd`) — and all seven now have producer-side automation: nightly / weekly / monthly are cron-driven; alpha / beta / release-candidate are push-driven on each respective long-lived branch; stable comes from release-please-action (or version-bump.yml for manual hotfixes).

Long-lived channel branches (force-push-protected): `nightly`, `weekly`, `monthly`, `alpha`, `beta`, `release-candidate`. Stable is `main`. Branch protection lives in two split rulesets — `.github/rulesets/protected-stable-branches.json` (main / release-candidate / beta / alpha — no bypass actor, fast-forward only) and `.github/rulesets/protected-cron-channels.json` (nightly / weekly / monthly with admin-bypass for cron pushes). Both are applied by `apply-branch-rulesets.yml`. `auto-delete-merged-branches.yml` exempts the channel names from PR-merge cleanup.

- `nightly-release.yml` — cron `0 0 * * *`. Resets `nightly` to `main`, merges every `origin/feat/*` (skips conflicts, opens issue), bumps version to `X.Y.Z-nightly.YYYYMMDD` across package.json / tauri.conf.json / Cargo.toml, force-pushes `nightly`, creates tag → triggers `release.yml`.
- `weekly-release.yml` — cron `0 0 * * 0` (every Sunday at 00:00 UTC). Identical pipeline shape to nightly-release.yml; tag suffix is `-weekly.YYYYMMDD`. Concurrency group `weekly-release`. Conflict-issue label is `weekly-conflict`.
- `monthly-release.yml` — cron `0 0 1 * *` (1st of each month at 00:00 UTC). Identical pipeline shape to nightly-release.yml; tag suffix is `-monthly.YYYYMMDD`. Concurrency group `monthly-release`. Conflict-issue label is `monthly-conflict`.
- `alpha-release.yml`, `beta-release.yml`, `release-candidate-release.yml` — push-driven (no cron). Triggered on every push to the matching long-lived branch. Each computes a monotonic counter across all existing tags of its channel (`vX.Y.Z-alpha.N` / `-beta.N` / `-rc.N`), bumps version manifests, commits, and pushes the tag — which triggers `release.yml`. Counters never reset across base-version bumps. Concurrency: per-branch lock prevents two pushes from racing into the same `N`.
- `release.yml` — derives the `prerelease` flag from the tag suffix at runtime (`prerelease: contains(tag, '-')`). Builds for any tag matching `v*`. **Auto-publishes prerelease drafts** at the end of `finalize-release` (#646); stable drafts stay as drafts pending maintainer review (`release-please-action` publishes them on the standard path; `version-bump.yml` pre-creates the release object via #645 to avoid the platform-job fragmentation that affected v0.49.1 / v0.49.2).
- `realign-alpha.yml` — one-shot helper to fast-forward `alpha` back onto `main` after a stable cut.
- `update-security-policy.yml` — runs on push to main and on tag push. Rewrites the "Supported Versions" table in SECURITY.md between sentinel comments, so the policy file always reflects the latest released version (currently v0.49.2).
- `check_all_updates` filters GitHub releases by `user_channel`. A Stable user hits `releases/latest`; non-Stable channels hit `releases?per_page=20` and pick the newest entry whose channel is `>=` the user's selected channel (so a user on Beta sees Beta, RC, and Stable — channel promotion, not exact-match).
- `download_and_install_app_update` in `commands/updates.rs` refuses tags whose channel is `< user_channel`. Even a tampered manifest or stale deep link can't downgrade stability.
- Channel-bump UI in `GeneralTab.tsx` gates Nightly/Weekly/Monthly/Alpha behind `dev_access_enabled` (Konami sentinel). `ChannelSwitchWarning.tsx` prompts on switch to a pre-release channel.
- Legacy `check_pre_releases: bool` is implicitly enabled whenever `update_channel != Stable`.
- **`ci.yml` gates alpha/beta/rc PRs, not just main** (#1043): before this, PRs targeting the long-lived channel branches — where all active development actually lands — only ran actionlint/static-security/pr-security, with zero Backend/Frontend/clippy/test gating. `pull_request.branches` now includes `alpha`, `beta`, `release-candidate` alongside `main`.
- **ELI5 release-notes self-heal gate** (#1046): prerelease GitHub Release bodies that regress to raw commit-speak (e.g. from a squash-merge losing the `Release-Note:` trailer) are now detected and auto-repaired rather than shipping silently broken; see "MANDATORY: user-facing (ELI5) release notes" above for the full mechanism.
- **alpha↔main realignment (EPIC #1040, 2026-07-24)**: `alpha` was reconciled with `main` at the content level — see `.claude/memory/project_alpha_main_drift.md` and `.github/HANDOFF.md` for the live phase tracker. Audit trail in `.github/audits/alpha-main-drift-content-analysis-2026-07-24.md` and `.github/audits/alpha-main-realignment-runbook-2026-07-24.md`. Ancestry closure (`-s ours` merge of main) and promotion to main are gated on maintainer go-ahead — do not run `realign-alpha` or a naive `git merge main` in the meantime.

### Conserving GitHub Actions Minutes

All workflows (CI, Changelog, Release Please, Release, Nightly Release, Apply Branch Rulesets) support both automatic triggers and manual `workflow_dispatch`.

**Do NOT use `[skip ci]`** in commit messages unless the user explicitly requests it. Every push must trigger CI to validate changes. Manual triggering is available if needed:

```bash
gh workflow run "CI" --ref main
gh workflow run "Release Please" --ref main
gh workflow run "Changelog" --ref main
gh workflow run "Release" -f tag=v0.3.3             # Release requires a tag input
gh workflow run "Nightly Release" --ref main        # Optionally -f dry_run=true
gh workflow run "Apply Branch Rulesets" --ref main  # Re-applies protected-branch ruleset
```

Note: `workflow_dispatch` only shows a "Run workflow" button in the GitHub UI for workflows whose YAML is on the default branch. Until a new workflow is merged to `main`, trigger it via the CLI with an explicit `--ref`.

### Release Please Branch Naming

Release-please v4 creates PR branches with the format:
`release-please--branches--{target}--components--{component}`

For this project (component name from `package.json` `name` field):
`release-please--branches--main--components--meedyadl`

The `.release-please-manifest.json` must match the current version to avoid release-please trying to create releases from an old version.

## Planned Service Integrations

| Milestone | Version | Service     | Engine                                                             | Key Notes                                                                       |
| --------- | ------- | ----------- | ------------------------------------------------------------------ | ------------------------------------------------------------------------------- |
| M8        | v2.0.0  | BBC iPlayer | [get_iplayer](https://github.com/get-iplayer/get_iplayer) / yt-dlp | Region-restricted (UK VPN may be required); yt-dlp as fallback engine           |
| M9        | v2.1.0  | Spotify     | [votify](https://github.com/glomatico/votify)                      | pip install, subprocess calls like GAMDL; adds Ogg Vorbis codec support         |
| M10       | v2.2.0  | YouTube     | [yt-dlp](https://github.com/yt-dlp/yt-dlp)                        | pip install, shared with BBC iPlayer; video-first service with format selection  |

Architecture foundation completed (#107, issues #314-#321):

- **`MediaServiceId` enum** (renamed from `MusicServiceId` in #314) with 5 services: AppleMusic, YouTubeMusic, YouTube, Spotify, BbcIPlayer
- **`EngineRegistry`** in `engine_registry.rs` — runtime query layer for `engines.toml` with `resolve_engine()`, `resolve_engine_chain()`, `detect_platform()`
- **`EngineCommandBuilder` trait** + `run_engine()` in `engine_runner.rs` — service-agnostic subprocess spawning with stub builders for Votify, yt-dlp, get_iplayer
- **Service-aware URL parser** (`detectService()`, `parseMediaUrl()` in `url-parser.ts`) detects service from URL and routes to correct engine
- **Service-aware download queue** — `QueueItem` carries `service`/`engine` fields; Apple Music enrichment guarded behind service check
- **`PerServiceSettings`** in `settings.rs` — nested per-service config with `engine_priority` overrides per platform
- **Engine fallback** via `try_engine_fallback()` for multi-engine platforms (e.g., BBC iPlayer: get_iplayer → yt-dlp)
- **`get_engine_config` IPC command** exposes full engine/platform config to frontend

Remaining per-milestone work:

- **Shared dependency management** — yt-dlp installed once, shared by YouTube and BBC iPlayer
- **Per-service settings UI tabs** — currently infrastructure only, needs React Settings page integration
- **Multi-service UI polish direction** (#911 — OnTheSpot-inspired, codebase-verified 2026-06-07, revised same day for responsive columns). Locks in the visual / interaction language for the queue, settings, and sidebar once M8-M10 services land. Anti-patterns (DO NOT regress): (1) **use responsive column visibility, never fixed density at all widths** — the queue may surface up to ~8 columns at wide widths but visibility must tier at Tailwind breakpoints (`md` 768 / `lg` 1024 / `xl` 1280 / `2xl` 1536) so a narrow window stays scannable; Tier 1 (album art, Artist — Album — Track, status pill, hover-revealed actions) always visible; Tier 2 platform icon at `md`; Tier 3 inline progress + speed/ETA at `lg`; Tier 4 codec/quality + content type at `xl`; Tier 5 file path + timestamp + size at `2xl`; click-to-expand affordance reveals hidden columns on demand at any width; (2) status pills (green/amber/red) and service brand colours (Apple red, Spotify green, YouTube red, BBC pink) must use distinct hue ranges so deuteranopia users can tell "Spotify queued" from "Spotify failed"; (3) never auto-open service-specific Settings tabs on URL paste — surface a per-service preview card under the textarea instead; (4) selection augments per-row actions, never replaces them. Rollout: Phase 1 (pre-M8 polish — **#911-0 responsive column system lands first as the foundation**, then per-row platform icon, album-art thumbnails, Artist — Album — Track row, status pills, hover-reveal actions, micro-animations, WCAG-AA service brand colour tokens, undo for destructive ops, per-service download preview card) → Phase 2 (multi-service-gated — service filter chips, multi-service empty state, unified Settings > Accounts page, sidebar per-service connection strip, first-launch service picker) → Phase 3 (Cmd/Ctrl+K command palette + batch selection toolbar) → Phase 4 (sidebar service grouping — deferred, filter chips solve same problem cheaper). Per-row vocabulary (responsive column system → platform icon → art thumb → Artist — Album — Track → status pill → hover-revealed actions) must be reused everywhere rows appear (Queue, History, Library Scan, future Cmd-K results panel) so users learn one format. Platform icon files live at `public/icons/platforms/<service>.svg` (inline SVG, `currentColor` fills so theme tokens apply); service brand colour CSS custom properties are named `--service-<service-kebab-case>` and registered under Tailwind `theme.colors.service.<service>`. New services land their icon + colour token + audit doc in lockstep. Don't propose UI that violates the anti-patterns without explicit user sign-off.

### Future Ideas

- **Native SwiftUI UI for macOS** — replace the web-based Tauri frontend on Apple Silicon with a fully native SwiftUI interface for tighter macOS integration and performance (no target version)
- **Enhanced Apple Music (MusicKit) Integration** — server-side token generation via MeedyaDL API (Cloudflare Workers initially, internal API long-term), removing the requirement for users to provide their own Apple Developer credentials. Apple DPLA Sections 2.1/2.8 prohibit embedding `.p8` private keys in distributed apps. Target: v2.x/v3.x. See `Dev_Notes.md` for architecture details.

## Build Targets

| Platform | Architecture | Format | Notes |
| -------- | ------------ | ------ | ----- |
| macOS | Apple Silicon (ARM64) | `.dmg`, `.app` | Requires macOS 13.3+; needs `xattr -cr` for unsigned builds |
| Windows | x64 (64-bit) | `.exe` (NSIS) | Also works on ARM64 via emulation |
| Windows | ARM64 | `.exe` (NSIS) | Native ARM64 build |
| Linux | x64 | `.deb`, `.AppImage` | Also works on ChromeOS via Crostini |
| Linux | ARM64 | `.deb` | Experimental; Pi 4/5, ARM servers |
| Linux | ARMv7 | `.deb` | Experimental; Raspberry Pi 32-bit |

## Build Commands

```bash
npm run dev          # Start frontend dev server
npm run build        # Build frontend
npm run type-check   # TypeScript type checking
npm run test         # Run Vitest tests
cargo check          # Check Rust compilation (in src-tauri/)
cargo tauri dev      # Run full Tauri dev mode
cargo tauri build    # Build release binary
```

## Important Notes

- Rust env: `export PATH="$HOME/.cargo/bin:$PATH"` (not `source "$HOME/.cargo/env"` — fails in zsh sandbox)
- Icons generated from `assets/icons/app-icon.svg` via `scripts/generate-icons.mjs` (requires `sharp` — install temporarily with `npm i sharp`)
- All settings stored as JSON in platform app data directory
- GAMDL config.ini is synced from GUI settings
- CSP in `tauri.conf.json` must include `connect-src ipc: http://ipc.localhost` for IPC
- Vite build config uses `TAURI_ENV_PLATFORM` for platform-specific JS targets (safari16.4 / chrome111)
- `devtools` Cargo feature enabled for WebView inspection in release builds
- **macOS codesign `--timestamp` workaround**: Tauri's `tauri-macos-sign` crate omits `--timestamp` from `codesign` calls, causing non-deterministic notarization failures ([tauri#11992](https://github.com/tauri-apps/tauri/issues/11992)). Both `release.yml` and `pre-release.yml` include a PATH-based wrapper script (Step 8.9) that intercepts `/usr/bin/codesign` and injects `--timestamp` automatically. Remove the wrapper once the upstream Tauri fix lands.
