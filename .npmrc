# Temporarily allow legacy peer dependency resolution.
# Required because eslint-plugin-react-hooks@7.0.1 declares
# peerDependencies.eslint: "^9.0.0" but we use ESLint 10.
# Remove this file once eslint-plugin-react-hooks ships
# a stable release with ESLint 10 support.
legacy-peer-deps=true
