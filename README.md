# Gamdl GUI

A cross-platform GUI for managing GAMDL and its dependencies.

## Features
- Automatically downloads and installs required libraries.
- Allows users to specify custom paths for dependencies.
- Checks for updates to GAMDL and its libraries.

## Installation
1. Clone the repository.
2. Install dependencies:
   ```bash
   pip install -r requirements.txt