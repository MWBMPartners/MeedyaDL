<svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
  <!--
    OF-Scraper platform icon — padlock (subscription/gated content).
    Generic enough to not reference a specific service by name.
    Uses currentColor for theme adaptability.
  -->
  <!-- Lock body -->
  <rect x="5" y="11" width="14" height="10" rx="2" fill="currentColor" fill-opacity="0.7"/>
  <!-- Lock shackle -->
  <path d="M8 11V8a4 4 0 0 1 8 0v3" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" opacity="0.5"/>
  <!-- Keyhole -->
  <circle cx="12" cy="16" r="1.5" fill="currentColor" fill-opacity="0.3"/>
</svg>
